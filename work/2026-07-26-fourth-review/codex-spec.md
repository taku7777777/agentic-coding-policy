# 第4巡レビュー実装仕様(Codex 向け)

第4巡レビュー(独立コンテキスト・2026-07-26)の指摘を反映し、`policy.md` を第5版にする。
編集対象は `policy.md` / `review.md` / `proposal.md` / `issue.md` の4ファイルのみ。
`README.md` と `work/` 配下は変更しない。既存の文体(断定調、太字・§参照の使い方)に合わせる。

---

## 1. policy.md — 第5版化

### 1.1 冒頭ブロック

- 5行目: 「更新: 2026-07-25(第4版)」→「更新: 2026-07-26(第5版)」
- 7行目(第4版の主な変更の段落)の**前**に、次の段落を挿入:

> 第5版の主な変更(第4巡レビュー反映): 形態2 の手順・完了条件に `種別: 記録` マーカーの付与を追加(§5.3・§11。マーカーが無いと §2.1 の判別で昇格直後の A が L と誤判定され上書きされるため)/ 既知名以外の新設容器にもマーカーを必須化(§5.6)/ §12 の観測期限を制定日起点から**配布日起点**に変更し、git 監査に昇格・流出の分類手順と `-uall` を追加(§12)。指摘と裁定は `review.md` §10。

### 1.2 §5 — 形態2 のマーカー付与(指摘1)

- §5 の2形態の表、形態2 の「行為」セル末尾に追記: 「。移した先の冒頭に `種別: 記録` を付す(§5.3)」
- §5.3 の表、形態2 の完了条件セルを次に置き換え:

> 成果物が `work/` の外へ移動し、冒頭に `種別: 記録` と `日付: / 状態:` 行が付され(移動先が A の容器の中である場合は不要)、**かつ** §5.1 の参照文が書かれたこと。**すべて**が必要

- §5.3 の表の直後(「最後に、変更したファイルのパスを…」の段落の前)に1段落追加:

> マーカーを付すのは §2.1 の判別を成立させるためである。形態2 で出した文書は A の容器の外に置かれることがあり(§5 の実測2例はどちらも容器の外)、マーカーが無いと次の更新で L と判定され、当時の記録が上書きされる。第4版まではこの手順が欠けていた(`review.md` §10)。

### 1.3 §5.6 — 新設容器のマーカー(指摘2)

- 「`docs/adr/` が既定なのは…」の箇条書き項目の直後に、箇条書き項目を1つ追加:

> - 既知名(`docs/adr/`・`CHANGELOG.md`)以外の名前で容器を新設するとき(`docs/BENCH.md` 等)は、冒頭メタ行に `種別: 記録` も置く。§2.1 の判別1 が効くのは既知名だけであり、マーカーが無いとリポジトリ固有の容器は L と誤判定されるため

### 1.4 §11 — スニペット v5(指摘1・2 の反映)

- 版コメント: `<!-- artifact-policy v4 (2026-07-25) — 本体: … -->` → `<!-- artifact-policy v5 (2026-07-26) — 本体: agentic-cording-policy/policy.md -->`
- スニペット内、権威ある場所へ書く条件2 の「**無ければ `docs/adr/NNNN-<slug>.md` を新設**(冒頭に `日付: / 状態:` 行を置く)」の直後に追記: 「。別名の容器を新設するなら冒頭に `種別: 記録` も置く」
- スニペット内、昇格の形態2 の項の「→ 完了条件は「移動した」**かつ**「参照文を書いた」の両方」を次に置き換え:

> 移した先のファイル冒頭に `種別: 記録` と `日付: / 状態:` 行を付す(`docs/adr/` 等の追記型容器の中に出す場合は不要)
> → 完了条件は「移動」「マーカー」「参照文」のすべて

(インデント・箇条書きの形は前後の行に合わせる)

### 1.5 §12 — 計装の修正(指摘3・4・5・6)

- 手順2 の untracked コマンド: `git status --porcelain | grep '^?? '` → `git status --porcelain -uall | grep '^?? '` とし、その項の末尾に括弧書きを追記: 「(`-uall` が無いと未追跡ディレクトリが `?? dir/` に畳まれ、中の md が見えない。実測故障の `rebuild-doc/` 21本がこの形だった)」
- 手順2 の末尾(補助フックの説明の後)に1文追加:

> 監査で出た `work/` 外の md は流出とは限らない —— 形態2 の昇格も「`work/` 外への追加」として同じ形で現れる。各ファイルについて §5.1 の参照文の有無を確認し、**昇格 / 流出** に分類してから手順5 の判定に使う。

- 手順3: 「観測期間: **2週間(〜2026-08-08)または新規タスク5件の早い方**」→

> 観測期間: **スニペット配布日から2週間、または新規タスク5件の早い方**。配布は 2026-07-26(→ 〜2026-08-09)。(第4版は制定日起点の「〜2026-08-08」としていたが、配布が済むまで窓が黙って消化されるため配布日起点に改めた)

- 手順7 の末尾に括弧書きを追記: 「(PreToolUse フックも PostToolUse と同様 Claude Code にしか効かず、Codex・人手の書き込みには効かない。手順2 の限界がそのまま残る)」

## 2. issue.md — 誤字修正(指摘8)

行の追加・削除はしない(行番号を保つ)。

- 9行目: 「過去に何度か対対応をしてきたが」→「過去に何度か対応をしてきたが」
- 14行目: 「過程で得られて知見や成果物」→「過程で得られた知見や成果物」

## 3. proposal.md — 第4版の注記(指摘9)

- 冒頭の「第3版で変わった箇所には該当箇所に「(→ 第3版で…)」の注記を入れてある(…)」の文の直後に追加: 「第4版の変更(スニペット配布先の `AGENTS.md` 化)には「(→ 第4版で…)」の注記を §10・§15-3 に入れてある。」
- §10 の見出し「## 10. 実装 — CLAUDE.md スニペット」の直下、本文段落の前に1行追加:

> **(→ 第4版で配布先を `AGENTS.md` 正 + `CLAUDE.md` は `@AGENTS.md` の1行に変更。`policy.md` §11)**

- §15 の手順3 「`CLAUDE.md` スニペットの配布(実装は Codex)」の末尾に追記: 「 **(→ 第4版で `AGENTS.md` 正に変更)**」

## 4. review.md — §10 第4巡の追加

- 3行目の更新・状態: 「更新: 2026-07-25(第3巡) / 状態: **第3巡まで反映**」→「更新: 2026-07-26(第4巡) / 状態: **第4巡まで反映**」(続く括弧内の説明は変更しない)
- ファイル末尾に以下のセクションを追加(内容は変えずそのまま。表の体裁は既存 §9 に合わせる):

```markdown
---

## 10. 第4巡 — 規則の相互作用と配備実態(2026-07-26)

第4版に対する単独レビュー1本(独立コンテキスト)。対象は個々の規則の中身ではなく、**規則同士の
相互作用**(判別規則 §2.1 と昇格手順 §5 の整合)と、§12 計装の**配備実態**(ファイルシステムの実測)。
設計本体(3分類・昇格2形態・注記方式)には今回も有効な反証が出なかった。

### 採用した指摘

| # | 指摘 | 裁定 |
|---|---|---|
| 1 | **形態2 の手順にマーカー付与が無く、昇格直後の A が §2.1 で L 判定になる。** §2.1 の判別は「A の容器の中 or 冒頭 `種別: 記録`」だが、形態2 の行為(§5)・完了条件(§5.3)・スニペット(§11)のいずれもマーカー付与を指示していない。実測2例(`metsuke: docs/08-dashboard-fb.md` / `sekisho: docs/design/synthesis-draft.md`)はどちらも容器の外にあり、この形の昇格産物は次の更新で上書き可と判定される。用語表(§1)には「容器の外の A 文書はマーカーを持つ」という規範があるのに、それを成立させる手順が存在しなかった | **採用**。§5・§5.3 にマーカー付与を追加し、§11 に反映(第5版) |
| 2 | **既知名以外の新設容器が L 判定になる。** §5.6 は `docs/BENCH.md` 等の新設を認めるが、§2.1 の判別1 が効くのは既知名だけで、新設容器へのマーカー要求も無い | **採用**。既知名以外の新設容器に `種別: 記録` を必須化(§5.6) |
| 3 | **配布が未実施のまま観測期限が制定日起点で消化されている。** 実測(2026-07-26): `metsuke/AGENTS.md`・`metsuke/CLAUDE.md`・`~/.codex/AGENTS.md` のいずれも不存在。一方フック(`~/.claude/hooks/artifact-policy-log.sh`)と `/wrap` は配備済み・稼働中(`md-writes.jsonl` に当日のログあり)。期限「〜2026-08-08」は制定日 07-25 起点なので、配布が遅れた分だけ観測窓が黙って縮む | **採用**。期限を配布日起点に変更(§12-3)し、配布を 2026-07-26 に実施 |
| 4 | **git 監査が形態2 の昇格と流出を区別できない。** どちらも「`work/` 外への md 追加」として同一の出力に載る。判定(§12-5)は3値なのに、計装の出力は成功と失敗の混合リスト | **採用**。§5.1 の参照文の有無で昇格/流出に分類する手順を §12-2 に追加 |
| 5 | `git status --porcelain` は未追跡ディレクトリを `?? dir/` に畳み、中の md が見えない。実測故障 #4(`rebuild-doc/` 21本)がまさにこの形で、現行コマンドは自分が検出したいはずの実例を検出できない | **採用**。`-uall` を付す(§12-2) |
| 6 | §12-7 の PreToolUse 警告フックへの昇格経路も Codex・人手の書き込みには効かない(PostToolUse と同じ限界)のに、§12-2 と違い注記が無い | **採用**。§12-7 に注記 |
| 7 | **自己適用違反(実測)**: `docs/cowork/0002-policy-redesign/intent.md` — 未記入テンプレート(プレースホルダ5行)が権威ある場所に untracked で存在。故障 #3(権威ある場所への流出)+ #4(untracked 放置)の、制定翌日・自リポジトリでの再演 | **採用**。裁定は生成元の調査結果を待って確定(TODO: 調査後に更新) |
| 8 | `issue.md` の誤字2件(L9「対対応」/ L14「得られて知見」)。L14 は全文書が参照するアンカー行 | **採用**。修正(行の追加・削除をせず行番号を保つ) |
| 9 | `proposal.md` に第4版の注記が無い。第3版の変更には「(→ 第3版で…)」を入れたのに、第4版の変更(配布先の `AGENTS.md` 化)は §10 見出し・§15-3 が当時のまま無注記。§6 の運用を自書に適用するなら第4版分も要る | **採用**。2箇所に注記 |

### 記録のみ(今回は動かさない)

| 指摘 | 扱い |
|---|---|
| リポジトリ名 `agentic-cording-policy` の "cording" は "coding" の誤綴り | 公開済みリポジトリの改名はリンク切れとのトレードオフ。ユーザー判断に委ねる |
| `policy.md` は規範文と導出メタ(撤回の経緯・誠実性の開示)が織り交ざり、運用のためだけに読む読者のコストが高い | 誠実性の開示は設計意図なので削らない。視覚的分離(注ブロック化)は次版以降で判断 |
```

---

## 完了確認(Codex 自身が行うこと)

1. 4ファイルの diff を確認し、仕様に無い変更が混ざっていないこと
2. `issue.md` の行数が 14 行のまま変わっていないこと
3. `policy.md` 内の「§10」「§12」等の参照が壊れていないこと
4. 変更したファイルのパスを応答内で1行報告する(このリポジトリのポリシー §5.3)
