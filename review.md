# レビュー結果と裁定

種別: 記録 / 日付: 2026-07-25 / 更新: 2026-07-30(第5巡) / 状態: **第5巡まで反映**
(§4(b)「ファイルごとの判断なしに1本のルールで決着する」を**撤回** → `policy.md` §5.1。
事実誤り9件を訂正。うち判断に関わる2件(§1 表の `plan.md`、§4(b))は注記付きで撤回し、
数値のみの7件は値を差し替えた —— **後者は §6「当時の本文を残し注記する」を満たしていない**。
誤った数値を残す価値がないと判断したが、規約上は例外である)

**公開時の匿名化(2026-07-25)**: 非コードの個人リポジトリ2本を `priv-A` / `priv-B` に置き換え、
内容が特定できるファイル名・引用を伏せた。本数・行数・日付は実測値のまま。これも本文の上書きであり
§6 の例外にあたる(記録の主張は変わらないため注記のみとする)。

`proposal.md` 初版に対する独立レビュー3本(運用の現実 / エージェント追従性 / 敵対的整合性)と棚卸し2本の結果、および各指摘への裁定。

レビューは互いのコンテキストを共有しない独立エージェントが実施。担当視点を分離し、重複を避けた。

---

## 1. 初版で誤診していたもの【最重要】

初版は5つの「故障」を挙げたが、**うち2つは散乱ではなく、意図的に維持されている記録だった。** ファイル名のパターン(`-fb`, `-draft`)から放置を推定し、参照関係を確認していなかったのが原因。

### 誤診1: `metsuke,ccm-ex: docs/08-dashboard-fb.md`

一次資料で確認した事実:

```
docs/08-dashboard.md:620    ## 17. `08-dashboard-fb.md`への対応     ← FB全項目の対応表
docs/08-dashboard.md:626    | S3 | …**2026-07-25更新**: ADR 0012がこの結論を撤回…（08-dashboard-fb.md S3も参照）|
docs/09-dashboard-implementation-plan.md:6   レビュー入力は[08-dashboard-fb](08-dashboard-fb.md)とする  ← 構造的参照
docs/08-dashboard-fb.md:68  **結末（2026-07-25）**: 当初はこの指摘どおりSSR-MPAを採用した…撤回した
git log: 3eb4c06(07-21 生成) → 1a361ee(07-25 更新)
```

**放置された影ではなく、L側から構造的に参照され、決定が覆るたびに追記される生きた記録**だった。生成4日後の本日も更新されている進行中の作業文書を「永久に残った」証拠として使っていた。

### 誤診2: `sekisho: docs/design/synthesis-draft.md`

```
docs/design/README.md:57            ### synthesis-draft.md — 反映前ドラフト(透明性のため保存)
docs/design/00-final-design-plan.md:9   > - 反映前のドラフト: synthesis-draft.md（透明性のため保存）
docs/design/review/independent-review.md:5  独立コンテキストが synthesis-draft.md を批判的にレビューした結果
```

**3箇所から理由付きで参照されている。しかも commit 済み。** 初版 §6 は「commit 済みなら失われないので消すコストがゼロになる」と主張し、その根拠に「sekisho が残したのは消したら失われると思ったから」を挙げたが、**実際には commit 済みの状態で「保存する」と明示的に宣言されていた**。§6 の因果説明は事実に反する。

### 有効なまま残る故障

| 故障 | 状態 |
|---|---|
| #3 リポジトリ直下への流出(`mrw: plan.md`, `review-*-20260716.md`) | 有効。ただし `plan.md` は第2巡で**要判断**へ(4本の L から参照。inventory §5-5) |
| #4 過程用の置き場が無い(`rebuild-doc/` 11本 + `rebuild-doc-fb/` 8本 untracked) | 有効 |
| #5 リポジトリ外の孤児(`~/github/` 直下4本) | 有効。ただし1本は生きた依存(後述) |
| #1' `mrw: review-fb-20260716.md` 系 | 有効(07-17以降リポジトリ全体が停止しており残存が確定) |

---

## 2. 初版が禁止していた機構が、サンプル中で唯一生きていた【設計変更を要する】

初版 §10 は status フィールド / supersedes チェーンを「git の上にもう一つバージョン管理を実装する行為。受益者が人間でない」として却下した。一次資料で確認した結果、**これは `metsuke` の ADR 運用そのものであり、そして唯一維持され続けている機構だった。**

```
adr/0003-no-dashboards.md:3   日付: 2026-07-17 / 状態: **部分的に置換**（2026-07-21・ADR 0011）
adr/0011-local-dashboard.md:3 日付: 2026-07-21 / 更新: 2026-07-25 / 状態: 採択・実装済み（S3はADR 0012が撤回）
adr/0011-local-dashboard.md:8 「本文は当時の決定として残し、該当箇所に注記する」   ← 規約として明文化済み
adr/0011-local-dashboard.md:38,83  **（→ [ADR 0012](0012-dashboard-client-rendered.md) で撤回）** ← インライン注記
adr/0012-…md:52               「撤回するのは結論であって、動機ではない」
git log 0011: 3eb4c06(07-21) → 4cd2f25(07-21) → 1a361ee(07-25)   ← 2回更新されている
```

`0003(作らない) → 0011(部分置換) → 0012(S3のみ撤回)` の方針転換の連鎖が、本文を読むだけで追える。**これは `issue.md` L7「調査や検証の結果として得られた発見をもとにより確度の高い方針に転換する」(2026-07-30 注記: 引用は原文と不一致 —— 原文は issue.md L7)への直接の答え**であり、初版はそれを禁止していた。

却下の理由は co-working の「捨てるもの」から持ち込んだが、そこでの却下理由は**複数人の coworkd が書き込むと衝突する**ことであり、単独作業には当てはまらない。独立設計を選んだ以上、この却下は引き継ぐべきでなかった。

**裁定**: §10 の却下を「**別ファイルとして生成される索引のみ**」に縮小。文書内の `状態:` 注記と supersede 注記は正式に採用し、`0011:8` の規約文をそのまま採る。

副次的に、これは初版 §4.2「`docs/adr/` は追記型(A)として運用され、**上書きされていない**」が事実に反することも示す(0011 は2回変更されている)。ADR は「集合としては追記 / 個々のファイルは上書き」のハイブリッドである。

---

## 3. 撤回する主張

| # | 初版の主張 | 反証 | 裁定 |
|---|---|---|---|
| 1 | §4.1「3値で閉じている。**増えることがない**」 | `work/<date>-<slug>/plan.md` は複数日タスクで**上書きされ、かつタスク完了で終端する**。空のはずの象限に提案自身の代表例が入る。ADR も個々は上書きされる | **撤回**。「証明された網羅的分類」から「実用的な3分類。境界に残余がある」へ格下げ |
| 2 | §6「commit 済みなら消すコストがゼロになる。**これが決定的**」 | `synthesis-draft.md` は commit 済みで、かつ消されなかった(§1 誤診2) | **撤回**。§6 全面書き直し |
| 3 | §4.3「既定をCにすれば『docsに入れるべきか』という判断が発生しない(基準2)」 | 判断は消えていない。§5 の昇格に移動し、量は**増えている**(該当箇所の特定・書き換え・棄却理由の1文・引用規律)。失敗事例の行為コストは「FBを docs に置く」= 1操作 | **撤回**。「判断を昇格時に移す。移した先のコストは意図的に払う」に書き換え |
| 4 | §3 基準1 の実測根拠「3/3が更新ゼロ」 | (a) `notes` は git リポジトリではないので commit 単位で測れない。mtime では 07-07 に更新あり (b) `priv-A` は 07-10 に第2バーストがある。「更新ゼロ」ではなく「**commit ゼロ**」 (c) `priv-A: <主題>-materials/` の1,469行を、§3 では劣化の証拠、§7 では設計妥当性の証拠として**二重計上**していた。Cは終端してよいのだから更新停止は正常終了であり、劣化の根拠から除外されねばならない | **格下げ**。「実測から導いた」の看板を外し、根拠は `priv-B` 1件 + 経験則に縮小 |
| 5 | §10「定期棚卸し lint/cron は実測3リポジトリで死ぬことが実証済み」 | サンプルに lint/cron の実例は**0件**。存在しない機構を「実証済み」とは言えない | **格下げ**。「未検証の懸念」へ |
| 6 | §10「索引/一覧は剪定されず嘘をつく」 | `notes/GAPS-BACKLOG.md` は「完了項目は archived へ移管済み(第1波/第2波)」と複数波で剪定され、`TAXONOMY-GAPS.md` は「未対応項目はゼロ」に到達した(ファイル自体は32行で存続。空になったのはバックログ項目)。しかも初版 §7 はこれを L として肯定しつつ §10 で新規作成を禁止しており不整合 | **格下げ + 整合**。禁止は「生成される索引」に限る |

---

## 4. 採用する設計変更

### (a) 昇格は2形態

初版の「昇格 = L の書き換え」は単線モデルで、誤診2件が示した実際の運用を表現できない。

| | 形態 | 昇格の行為 | 実例 |
|---|---|---|---|
| 1 | **L の書き換え** | 本体を書き換え、棄却理由を本文に1文 | 通常のFB |
| 2 | **A への凍結** | 成果物は日付付き記録として残し、**L側がそれを指して「なぜ残すか」を書く** | `metsuke §17` / `sekisho README:57` |

**(→ 第3版で変更)** 形態2 の名称を「権威ある場所へ出す」に改め、成果物を **`work/` の外へ移す**ことを
必須にした(`work/` に残したままだと、権威ある場所が `work/` に依存し「読まずに消せる」が破れるため)。
完了条件も「移動した **かつ** 参照文を書いた」の両方になった。`policy.md` §5 / §5.3

### (b) 昇格の証拠は L→C 方向の参照に限る【矛盾解消のルール】

初版は §7 で `notes/20260706_*-review.md` を **A**(その日のレビュー結果=時点の事実)とし、§9(a) で「レビューFB」を **C** の代表例とした。同じものが A とも C とも読める矛盾。

解決するルール: **L の側から参照され、残す理由が書かれているものだけが A。逆方向(C→L)の参照は証拠にならない**(誰でも自分から権威ある文書を指せるので、維持する意思の証拠にならない)。

検証:

| | L→C の参照 | 判定 |
|---|---|---|
| `metsuke: 08-dashboard.md §17` → fb | あり(対応表 + 理由) | A |
| `sekisho: 00-final-design-plan.md:9` → draft | あり(「透明性のため保存」) | A |
| `notes: GAPS-BACKLOG.md / TAXONOMY-GAPS.md` → `20260706_*` | **0件**(実測) | **C**(3本すべて) |

ファイルごとの判断なしに1本のルールで決着する。
**(→ 第2巡で撤回。この3件は本ルールの導出元そのものであり、独立した検証になっていなかった。
導出集合外の唯一の例 `mrw/plan.md` ではルールと内容判断が食い違う。`policy.md` §5.1 / §10-2)**

### (c) A の定義を広げる — 知見(`issue.md` L14)の受け皿

初版 §5 は「調査結果の『**その時の解釈**』は昇格しない」と明記していた。`issue.md` L14 が求める「知見」= 再利用可能な学びは、その大半が解釈である(「この方式は X が理由で破綻する」)。**初版は L14 が求めるものを消す規約を積極的に定めていた。**

第4の分類は作らない。A の定義を広げる:

> **A = 時点の記録。当時の判断と、その根拠となった発見を含む。**

根拠は再び ADR。`0012:52`「撤回するのは結論であって、動機ではない」はまさに知見であり、`0011:8`「本文は当時の決定として残し、該当箇所に注記する」がその保存機構である。既に動いているものを規約化するだけで足りる。

### (d) `work/` が解くのは混乱であって蓄積ではない

初版 §6 は「消してよいと宣言する」で終わらせていたが、削除の契機が空白になる。正直に書く:

- **`work/` は「何が権威あるものか」の混乱を除去する。削除の判断を除去はしない**
- 昇格が A 形態になる成果物は、`work/` から出る動きが後から発生する(metsuke の FB がまさにそれ)。ここは避けられないコスト
- 削除は任意のまま。ただし `~/github/archived/` に**完了時に波でまとめて移管する**という運用が既に存在する(`GAPS-BACKLOG.md`「第1波 2026-07-05 / 第2波 2026-07-05〜07-06」)。これを終端の選択肢として明記する
- 「蓄積は無害」とは言わない

### (e) git 前提を明記

`notes` / `archived` / **この `agentic-cording-policy` リポジトリ自身**は git リポジトリではない(実測)。「commit する / 履歴に残るから消せる」は対象3箇所で成立しない。git 前提を規約に明記する。

### (f) `work/README.md` を置かない

`work/README.md` は「`work/` が存在する限り正しくあり続けねばならない」= L でありながらパスは `work/` = C。「パスが寿命を宣言する」という中核ルールが、提案が新設する2ファイルのうち1つで破れている。CLAUDE.md スニペットに一本化する。

### (g) `inventory.md` は `work/` に置く

初版 §13-4 は `inventory.md` を「参照用に置く」としたが、これは §10 が禁止した生成一覧そのもの。移行を実行しない以上、内容は生成時点で凍結され古くなる。**`work/2026-07-25-artifact-policy/inventory.md` に置いて C とする。**

---

## 5. スニペットの穴(次のパスで修正。今回は列挙のみ)

エージェント追従性レビューは18の入力例を作り、**迷わず即断できたのは4例のみ**と判定した。§9(a) スニペットに対する指摘:

| # | 穴 | 影響 |
|---|---|---|
| 1 | **A(積む)がスニペットに一文も無い** | CHANGELOG・ADR・進捗メモを分類する語彙をエージェントが持たない。最優先 |
| 2 | `<slug>` の生成規則が皆無 | 同タスクが `dashboard-review` / `review-dashboard-fb` に散り、「`ls work/` が索引になる」前提が work/ の内側で崩れる |
| 3 | 日付が「今日」か「タスク開始日」か未規定 | 複数日タスクが日付ごとに分裂 |
| 4 | 「引用する。要約しない」が自己検証不能 | LLMの既定挙動と正面衝突するのに検知手段が無い。→ 昇格文を原文に grep して一致確認する手順を入れる |
| 5 | 昇格の完了判定シグナルが無い | 次セッションが同じFBを見て二重対応する |
| 6 | 非文書成果物(スクリプト/ログ/データ/画像)の置き場が未定義 | 発生頻度は文書系と同等以上。「規定しない」はエージェントから見れば未定義動作 |
| 7 | `work/README.md` が生成されない | §9(b) がスニペットに含まれていない。→ (f) で解決 |
| 8 | 複数リポジトリ跨ぎの `work/` 配置が未規定 | |
| 9 | 並列サブエージェントの同一ファイル衝突 | 3観点レビューが全員 `review.md` に書いて後勝ちになる |
| 10 | ADR・handoff・棚卸し表・提案書自身が「迷う」ではなく**誤着地** | 表層キーワード一致で自信を持って誤った場所に書く。気づかれない分こちらが実害大。特に handoff は `work/<date>-<slug>/handoff.md` に迷わず書くが、**次セッションは slug を知らないので発見できない** |

---

## 6. 反証を試みて崩れなかったもの

| 主張 | 状態 |
|---|---|
| §4.1「種類分類は増える」 | 17種の観測を再確認(第2巡で `handoff` を除外)。`sekisho` だけで `p0〜p4-findings` + `p0〜p4-plan` + `runbook` + `session-*-progress` と増殖。**崩せなかった** |
| §4.4 日付書式の `YYYY-MM-DD` 統一 | ISO 8601 / `ls` ソート / 既に多数派。**崩せなかった** |
| §11 故障5 → 検討専用リポジトリ | 孤児4本の存在と、`co-working/` が既に同パターンであることを確認。対応表5行のうちこの行だけ反証されなかった |
| パスで寿命を宣言するというアイデア自体 | 反例なし。崩れたのは網羅性証明(§3-1)と例外(§4-f)であって、発想自体は**崩せなかった** |

---

## 7. 未検証の前提として明記するもの

| 前提 | 状態 |
|---|---|
| **CLAUDE.md スニペット1枚で root 流出が止まる** | **未検証**。`mrw/CLAUDE.md:5,32-41` には既に配置規約が存在する(`tasks/` は生成状態・commit しない等)。それでも `review-fb-*.md` `plan.md` `rebuild-doc*/` が root に生成された。既存規約は md 成果物を対象にしていないので直接の反証ではないが、「1行の規約を配れば着地点が変わる」は唯一の該当サンプルで支持されていない。→ **1リポジトリで先行適用し、root 流出が止まるか観測する**ステップを入れる |
| 削除の契機 | `work/` は削除の契機を持たない。`archived/` への波移管が唯一の実例 |
| 昇格の粒度 | metsuke は18項目(+軽微1行)をID単位で対応表化した。「1文書き足す」で足りるのか対応表を作るべきか、基準が無い |

---

## 8. 既存の C 置き場を見落としていた

`~/github/archived/` が既に C の墓場として機能している(実測):

```
archived/
├── 20260705_overall-review.md
├── 20260706_env-cases-expansion-plan.md
├── refactor-plan.md
├── GAPS-BACKLOG.md      ← 完了分の移管先
├── TAXONOMY-GAPS.md     ← 同上
└── cases/               ← 18群の内部精査メモ
```

`notes/GAPS-BACKLOG.md` に「対応が完了した項目は archived/GAPS-BACKLOG.md へ移管済み(第1波 2026-07-05 / 第2波 2026-07-05〜07-06)」と明記されており、**「洗い出し → 完了 → 移管」という昇格と終端が定義された運用が既に生きている**。しかも git 管理外なので、これは「git 履歴に残す」ではなく「**権威ある場所から動かす**」パターンである。

また `sekisho/.gitignore` には過程成果物の終端条件が明記されている:

```
# P0 spike run artifacts (ephemeral); RESULTS.md is the committed summary
# M1 spike run artifacts (ephemeral); FINDINGS.md summaries are committed
```

初版 §原因2「対応が終わったら何が残るべきかが決められていない」は、少なくともこのリポジトリでは成立しない(ignore パターン自体はログだが、コメントが `RESULTS.md`/`FINDINGS.md` を名指しで committed と規定しており、md の終端規約になっている。一般化は行き過ぎだった)。

---

## 9. 第3巡 — 実効性の層(2026-07-25)

第3版に対する単独レビュー1本。対象は規約の中身ではなく**「規約を届けて効かせる層」**。第1・2巡と異なり、3分類・昇格・注記の設計本体には有効な反証が出なかった。

### 採用した指摘

| # | 指摘 | 裁定 |
|---|---|---|
| 1 | **スニペットが Codex に届かない。** 実装の書き込み主体は Codex(グローバル CLAUDE.md で委譲を規定)だが、Codex が読むのは `AGENTS.md` であり `CLAUDE.md` ではない。実測: `~/.codex/config.toml` あり = Codex 稼働中、`~/.codex/AGENTS.md`・`metsuke/AGENTS.md`・`metsuke/CLAUDE.md` いずれも不存在。このままでは §10-1 の観測が「届いていないから変わらない」という自明な結果に汚染される | **採用**。§11 を「`AGENTS.md` 正 + `CLAUDE.md` は `@AGENTS.md`」に変更 |
| 2 | **L/A 判別子「`状態:` 行の有無」が自己適用で両方向に誤る。** 偽陽性: `policy.md` 自身(`状態:` 行を持つ L)が A 判定になり上書き禁止になる。偽陰性: `proposal.md`(「(→ 第3版で撤回)」注記を積む A 運用なのに `状態:` 行なし)と `CHANGELOG.md`(§1 が A の容器と明記するのに `状態:` 行なし)が L 判定になり上書き可になる | **採用**。判別子を「A の容器の中 or 冒頭 `種別: 記録`」へ置き換え(§2.1)。`状態:` は有効性表示に限定 |
| 3 | **パイロットの観測が無計装。** 誰がどう観測するか未定義で、人の記憶と手動棚卸しに依存 —— 基準1「保守を要求する仕組みは死にやすい」に自ら抵触する。観測期間・判定に必要なタスク数も未定義 | **採用**。git 監査(終了時1回)を主、ユーザーレベル PostToolUse フックを補助として §12 に計装を定義。期間は2週間または新規タスク5件。フックは Codex の書き込みを捕捉しない(Codex は Claude Code のツールを経由しない)ため、主計装は git 監査 |
| 4 | **§10-5(昇格未実行)を自己予測しながら緩和策がゼロ。** `issue.md` L14(知見の管理)の成否がここに全部懸かる | **採用**。タスク完了時の `/wrap` コマンド(昇格候補の列挙・提案。義務化はしない —— §9 の設計と整合) |
| 5 | 本リポジトリ自身が git 管理外で §7.2 に自ら違反(規約のマスターに履歴もバックアップも無い) | **対応済み**(2026-07-25 に git init + push) |
| 6 | §5.4 の完全一致 grep 検証は複数行引用で機械的に失敗する(md の折り返しを跨ぐ文字列は行単位の grep に一致しない) | **採用**。引用を行単位に限定(§5.4) |
| 7 | スニペットに L/A 判別規則が無く、「追記型の記録は上書きしない」を目の前の文書に適用できない | **採用**。判別1行をスニペットに追加(§11) |
| 8 | `proposal.md` L10 が存在しない「§16」を参照(参照切れ) | **採用**。参照先を修正 |

### 記録のみ(今回は動かさない)

| 指摘 | 扱い |
|---|---|
| 展開フェーズの配布は per-repo の N 枚複製ではなくグローバル1枚(`~/.claude/CLAUDE.md` + `~/.codex/AGENTS.md`)にすべき | パイロット成功後の展開設計で判断。§12-7 に第一候補として明記した |
| PreToolUse 警告フック(`work/` 外への md 書き込みをエージェントに差し戻す)への昇格 | パイロットで漏出が観測されてから。§9 の却下(定期棚卸し lint)には抵触しない —— イベント駆動・保守不要・fail-open |

---

## 10. 第4巡 — 規則の相互作用と配備実態(2026-07-26)

第4版に対する単独レビュー1本(独立コンテキスト)。対象は個々の規則の中身ではなく、**規則同士の
相互作用**(判別規則 §2.1 と昇格手順 §5 の整合)と、§12 計装の**配備実態**(ファイルシステムの実測)。
設計本体(3分類・昇格2形態・注記方式)には今回も有効な反証が出なかった。

### 採用した指摘

| # | 指摘 | 裁定 |
|---|---|---|
| 1 | **形態2 の手順にマーカー付与が無く、昇格直後の A が §2.1 で L 判定になる。** §2.1 の判別は「A の容器の中 or 冒頭 `種別: 記録`」だが、形態2 の行為(§5)・完了条件(§5.3)・スニペット(§11)のいずれもマーカー付与を指示していない。実測2例(`metsuke: docs/08-dashboard-fb.md` / `sekisho: docs/design/synthesis-draft.md`)はどちらも容器の外にあり、この形の昇格産物は次の更新で上書き可と判定される。用語表(§1)には「容器の外の A 文書はマーカーを持つ」という規範があるのに、それを成立させる手順が存在しなかった | **採用**。§5・§5.3 にマーカー付与を追加し、§11 に反映(第5版) |
| 2 | **既知名以外の新設容器が L 判定になる。** §5.6 は `docs/BENCH.md` 等の新設を認めるが、§2.1 の判別1 が効くのは既知名だけで、新設容器へのマーカー要求も無い | **採用**。既知名以外の新設容器に `種別: 記録` を必須化(§5.6) |
| 3 | **配布が未実施のまま観測期限が制定日起点で消化されている。** 実測(2026-07-26): `metsuke/AGENTS.md`・`metsuke/CLAUDE.md`・`~/.codex/AGENTS.md` のいずれも不存在。一方フック(`~/.claude/hooks/artifact-policy-log.sh`)と `/wrap` は配備済み・稼働中(`md-writes.jsonl` に当日のログあり)。期限「〜2026-08-08」は制定日 07-25 起点なので、配布が遅れた分だけ観測窓が黙って縮む | **採用**。期限を配布日起点に変更(§12-3)し、配布を 2026-07-26 に実施(→ 第5巡: metsuke の git 履歴では v4 配布 = `af31b45` 2026-07-25 20:57 が先行しており、左の実測はそれ以前の時点のもの。観測起点は初回配布 07-25 に訂正。§11) |
| 4 | **git 監査が形態2 の昇格と流出を区別できない。** どちらも「`work/` 外への md 追加」として同一の出力に載る。判定(§12-5)は3値なのに、計装の出力は成功と失敗の混合リスト | **採用**。§5.1 の参照文の有無で昇格/流出に分類する手順を §12-2 に追加 |
| 5 | `git status --porcelain` は未追跡ディレクトリを `?? dir/` に畳み、中の md が見えない。実測故障 #4(`rebuild-doc/` 21本)がまさにこの形で、現行コマンドは自分が検出したいはずの実例を検出できない | **採用**。`-uall` を付す(§12-2) |
| 6 | §12-7 の PreToolUse 警告フックへの昇格経路も Codex・人手の書き込みには効かない(PostToolUse と同じ限界)のに、§12-2 と違い注記が無い | **採用**。§12-7 に注記 |
| 7 | **自己適用違反(実測)**: `docs/cowork/0002-policy-redesign/intent.md` — 未記入テンプレート(プレースホルダ5行)が権威ある場所に untracked で存在。故障 #3(権威ある場所への流出)+ #4(untracked 放置)の、制定翌日・自リポジトリでの再演 | **採用**。調査の結果、cowork CLI がフックのたびに `docs/cowork/<task-id>/intent.md` を読み(パスはハードコード・設定変更不可)、`work/` への移動はツールの方針追跡を壊す。§3.3「ツールがパスを固定する成果物」を新設し、本件は記入して commit で解消(`metsuke: docs/cowork/0001-workshop-prep-002/intent.md` に「配置 → 記入 → git 追跡」の先例)。master 側チェックアウトの同名未記入コピー(untracked)はブランチ統合前に手動削除が必要 |
| 8 | `issue.md` の誤字2件(L9「対対応」/ L14「得られて知見」)。L14 は全文書が参照するアンカー行 | **採用**。修正(行の追加・削除をせず行番号を保つ) |
| 9 | `proposal.md` に第4版の注記が無い。第3版の変更には「(→ 第3版で…)」を入れたのに、第4版の変更(配布先の `AGENTS.md` 化)は §10 見出し・§15-3 が当時のまま無注記。§6 の運用を自書に適用するなら第4版分も要る | **採用**。2箇所に注記 |

### 記録のみ(今回は動かさない)

| 指摘 | 扱い |
|---|---|
| リポジトリ名 `agentic-cording-policy` の "cording" は "coding" の誤綴り | 公開済みリポジトリの改名はリンク切れとのトレードオフ。ユーザー判断に委ねる |
| `policy.md` は規範文と導出メタ(撤回の経緯・誠実性の開示)が織り交ざり、運用のためだけに読む読者のコストが高い | 誠実性の開示は設計意図なので削らない。視覚的分離(注ブロック化)は次版以降で判断 |

---

## 11. 第5巡 — 統合監査と実配備の検証(2026-07-30)

第5版に対する統合監査。互いにコンテキストを共有しない4本の並列監査(自己整合性 / 敵対的レビュー /
パイロット中間観測 / 基盤・伝達力)と、所見を統合した改善計画への懐疑パネル3本(自己整合性 /
費用対効果 / ユーザー意図)で実施。**拒否0件**・修正指示付きで計画を確定した。調査記録は
`work/2026-07-30-repo-strengthening/` の5本(C。本節は記録だけで自立する)。

設計本体(3分類・昇格2形態・注記方式)には5巡目も有効な反証が出なかった。崩れたのは
**版の運用**(git ref 間の権威分裂)と**観測の計装**の層である。

### 採用した指摘(第6版に反映)

| # | 指摘 | 裁定 |
|---|---|---|
| 1 | **配布コピーが本体を追い越した。** metsuke/AGENTS.md:1 は「artifact-policy v5 (2026-07-26)」、公開 master の policy.md は第4版のまま —— 第5版とその裁定(§10)は未マージ・未 push のブランチ `0002-policy-redesign` にしか存在しなかった。§11「スニペットは本書からの複製である。」が4日間虚偽化し、腐り検知は「コピーが古い」方向しか想定していないため検知されなかった。A の裁定が `git branch -D` 1回で消える状態でもあった | **採用**。ブランチを master へ統合(ff)し、**版の運用規則**を §11 に新設。git tag v3〜v5 を遡及付与(注釈付きタグ) |
| 2 | **観測起点が3通りに割れていた。** 第4版「〜2026-08-08」(制定日起点)/ 第5版「〜2026-08-09」(v5 配布日 07-26 起点)/ 実配布は v4 = `af31b45` 2026-07-25 20:57。v5 起点だと初日の書き込み(同日 23:51 の `docs/claude-code-cost-workshop.md` 442行、`fbcfbb7`、work/ 不経由)が監査から漏れる | **採用**。初回配布 2026-07-25 起点(〜2026-08-08)に確定(§12-2・§12-3)。§10-3 に訂正注記 |
| 3 | **git 監査が形態2 の「移動」を検出できない。** 補助計装(PostToolUse フック)は Write\|Edit の .md のみ記録し、`mv`/`git mv` を発火させない。主計装にも rename 検出が無かった —— 観測項目(iii)が原理的に観測不能だった | **採用**。`git log -M --diff-filter=R` を終了時監査に追加(§12-2) |
| 4 | **/wrap が第4版仕様のまま。** 形態2 の完了条件がマーカー無しの2要件で、第4巡-1 で修正した欠陥を観測用の計装自身が再生産していた。版表記も地の文に埋まり(「第4版」)、腐り検知の対象外。`head -5` が6件目以降の未昇格ディレクトリを黙って切り捨てる欠陥も | **採用**。v5 仕様(移動・マーカー・参照文)に更新し、版ヘッダを付与。判定は現在タスクのみとし、他は名前の列挙に変更(全件再判定は定期棚卸しへの転化なので行わない) |
| 5 | **§5.4 の引用規律の遡及違反2件。** proposal.md と本書 §2 の issue.md L7 引用が原文と完全一致しない(原文への grep 一致0件)。規律制定前の引用だが、権威ある場所に不正確な引用が残っていた | **採用**。両箇所に日付付き注記。意味は保存されており本文は書き換えない |
| 6 | **issue.md の分類が構造矛盾。** §2.1 判別では L(上書き可)だが、権威ある場所の8箇所以上が行番号(L7/L14)で参照しており、L として上書きした瞬間にアンカーが静かに壊れる。実態は 07-25 以来の凍結 | **採用**。冒頭メタ行で A に確定(空行 L2 を置換し、行番号を保存 —— 第4巡-8 と同じ方式)。§8 の図も A 側へ移動。**課題認識の上書き改訂権の放棄を含むため、ユーザーが異議あれば差し戻す** |
| 7 | **導出元の実測例がマーカー未付与。** metsuke/docs/08-dashboard-fb.md(形態2 の模範例)は容器外・マーカー無しで、配布済み v5 の判別では「直接上書きしてよい」対象 —— 当時形の記録が次の更新で破壊されうる | **採用**。§12-9 に1回きりの遡及マーキングを規定し、パイロット repo で実施(実施記録は本節末尾) |
| 8 | proposal.md の壊れた参照(「(§13)」は初版の節番号の残存。現 §11)と、mrw / ccm-ex 略記の未宣言 | **採用**。日付付き注記で対応(A の本文は書き換えない) |
| 9 | **本リポジトリ自身に AGENTS.md/CLAUDE.md が無い。** §11 が「これが実効性の本体」と呼ぶ配布物を、規約の自リポジトリ自身が欠く | **採用**。§8 差分メモとして最小の `AGENTS.md` + `CLAUDE.md`(`@AGENTS.md`)を配布。本体 policy.md が同居するため §11 スニペットは複製せず、版ヘッダも名乗らない(参照で足りる)。**これにより本リポジトリは補助計装の対照群から外れる** —— 終了時監査の対照群集計から除外すること |
| 10 | L 文書の stale 実測値(「計857行」は 07-30 実測で 992行に成長)と、work/ 内の棚卸しへの根拠言及が §3.1 のグレーゾーン | **採用**。実測日を付し、「第2版策定時の棚卸し(当時の作業記録)」と当時形に修正 |

### パイロット中間観測(2026-07-30。§12-2 の終了時監査の先取りではない)

- 観測項目(i): **着地する寄り(n=2)**。07-27 の新規タスク2件は §4 準拠で `work/` に着地。対照群 sekisho は work/ 使用0件のまま
- 観測項目(ii): 形態1 の昇格1件を確認(`work/2026-07-27-auto-mode-cost-attribution/findings.md` → docs 側6ファイル。docs から work/ への参照は grep 0件で §3.1 不変条件も維持)
- 観測項目(iii): 形態2 は0件 —— **「観測できない」**。§12-5 のとおり成功と読まない
- 流出候補1件: v4 配布と同日・約3時間後の `docs/claude-code-cost-workshop.md`(442行・work/ 不経由。追加は `fbcfbb7` 2026-07-25 23:51、配布は `af31b45` 同日 20:57)。ただし 07-27 に事実修正を受け維持されており L 解釈の余地が残る。L/C の確定は終了時監査で行う

### 第7版の候補(確定ではない。終了時監査 = 第6巡で観測結果と突き合わせて裁定する)

敵対的レビューの机上シミュレーション9本で「崩れた」規則層の穴。**すべて机上由来で、観測期間中の実害は
4監査のいずれにも未観測** —— §9 が実測0件の禁止に「弱い」を付す流儀に従い、観測前に確定させない。

- §5.5 の行同士の衝突: 実測付きの不採択案は「昇格しない」と「形態2(知見)」に同時該当し、分離も禁止されていて決定不能。優先規則候補「知見の有無を優先」 —— **候補中の最有力**(規則間の現実の矛盾)
- A の判定質問の過剰包含: バグ調査・進捗記録が判定質問を文字通り満たし、確信的に ADR/CHANGELOG 化する。射程フィルタ候補「タスク限りなら work/」
- 並列昇格の保証機構: 「採番を1エージェントに絞る」(§5.6)は主体・機構が未定義で、スニペットに規則が1行も無い。候補「採番・容器新設は親エージェントのみ」
- そのほか: 複数リポジトリ跨ぎの引き継ぎポインタ / CHANGELOG の一律 A 指定と keep-a-changelog の Unreleased 運用の衝突 / 非文書成果物(画像等)の昇格経路 / slug 語彙の再利用 / §7.1 への第4条件(自分以外の書き込みの有無)/ 版履歴・観測終了トリガの単発リマインダ
- `~/.claude/CLAUDE.md` への §8 1行の先行配置は、対照群を汚染するため観測期間中は見送り(§12-7 で再判断)

### 記録のみ(ユーザー判断に委ねる)

| 論点 | 状態 |
|---|---|
| リポジトリ改名(cording → coding) | 正名 `taku7777777/agentic-coding-policy` が**自分名義の空リポジトリ**として存在(2026-07-25 作成・以後未使用)。「本体:」ポインタが増えるほど改名コストは単調増加 —— 判断は早いほど安い。L 側の要更新参照は5箇所(README.md:1 / policy.md §11 ヘッダ / metsuke/AGENTS.md:1 / wrap.md / artifact-policy-log.sh) |
| LICENSE 不在 | 公開リポジトリが既定で all rights reserved のまま、§11 が第三者に複製を促す不整合。第一候補 CC BY 4.0。複製の摩擦ゼロを優先するなら CC0 1.0 |

### 実施記録(2026-07-30)

- ブランチ `0002-policy-redesign` を master へ ff 統合。注釈付きタグ v3(`4d7996f`)/ v4(`14040cb`)/ v5(`7e64e63`)/ v6(第6版)を付与し、master と全タグを push。統合前に「§11 スニペットブロック == metsuke/AGENTS.md」の完全一致を diff で確認済み
- 第6版では **§11 のスニペットブロックを変更していない**(版ヘッダは v5 のまま)。配布済み metsuke/AGENTS.md の再配布は不要であり、パイロットの観測条件は変わっていない
- §12-9 の遡及マーキングを metsuke で実施: `docs/08-dashboard-fb.md` の冒頭メタ行に `種別: 記録` を付与(コミット `2ae04ed` 2026-07-30 20:32)。**artifact-policy の管理作業であり、終了時監査では流出・昇格のいずれにも数えない** —— 補助計装の `md-writes.jsonl` には metsuke/outside の1件として載るため、クロス集計から除外すること
- `~/.claude/commands/wrap.md` を v5 仕様に更新(完了条件の3要素化・版ヘッダ付与・`head -5` 撤廃)。地の文の版表記は削除し、本体の版表記を参照する形に変えた(二重管理を作らないため)
- GitHub の description と topics を更新(`llm` / `claude-code` / `codex` / `agents-md` / `documentation`)
