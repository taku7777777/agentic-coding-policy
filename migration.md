# 移行ガイド — 既存の容器外 A の1回きり移行

**位置づけ**: [policy.md](policy.md) §12.1(規範)の運用ガイド。**本書は規範ではない** —— 守るべき
不変条件はすべて §12.1 にあり、矛盾したら §12.1 が勝つ。本書は手順の詳細と、適用で得た留意事項を
持つ(§11 の版発行基準により、本書の更新は版を要さない)。実測の出典は各リポジトリの実施記録
(§4 実例参照)。

## 1. 事前調査

- **全数の内容判定**(§12.1 の3手がかり)の実務:
  - 役割宣言: README の文書表と、ファイル冒頭の位置づけ行(「実測記録」「レビュー」「ステータス: 確定(日付)」)。
    「最新版」「唯一の管理場所」「以後もここに追記する」は L の信号
  - 履歴の不変性: `git log --follow --format='%ad %s' --date=short -- <file>` で初版日と最終変更日を取る
  - 冒頭 20行で判定できるものが大半。迷うものだけ深く読む
- **凍結判定(hot の除外)**: 「確定」宣言があっても、直近(目安2週間)に変更のあるファイルは動かさない。
  進行中フェーズの台帳・計画・契約は、フェーズ完了後に改めて判定する
- **後継・重複の横断確認**: 単一リポジトリの内部証拠だけでは導入・移行の判断を誤る。
  同名・同構成の docs を持つ別リポジトリ、README の退役宣言、Initial commit と最終コミットの日付の
  連続を確認する(実例: claude-code-monitoring-ex は docs 同一構成・ADR 同一番号・日付連続から
  metsuke の前身と判明し、移行対象から外れた)
- **被参照マップ**(§12.1 の不変条件): 移動対象ごとに
  `grep -rn "$(basename <file>)" --include='*.md' .` で repo 全体を走査する。
  主要な L 文書だけを見ると、**活動中の文書からの流入参照**を見落とす(実測: 見落とし4文書から
  壊れリンク11件)。地の文の言及とリンクを区別して控えておく(修正するのはリンクだけ)

## 2. 実施

- **mv → コミットは一連で行う。** `git mv` は自動でステージされるため、コミットまでの間に
  並行セッションがコミットすると rename がそちらへ混入する(実測: sekisho `30d61ff`)。
  並行セッションが稼働中のリポジトリでは静穏時に実施するか、mv 直後に即コミットする
- **`日付:` の遡及付与**: 最初の見出し行の直後に1行。値は初版コミット日 —— ただし
  **保留の解除条件が「commit」だった対象では初版コミット日を使わない**(必ず移行と同日になり、
  文書の実際の日付を潰す)。その場合は **本文の日付宣言(実施日 / 調査日 / 策定日)→ ファイル名の
  日付接頭辞 → ファイルの最終更新時刻** の順に採り、**どれを採ったかを注記に書く**。
  リンクを修正したファイルは注記を併記する —— 例:
  `日付: 2026-07-17(2026-08-02 の §12.1 移行で遡及付与。壊れる相対リンクのみ機械修正 —— §6 例外)`
- **移す単位はファイルではなくクラスタに上げる。** 記録に従属する資産群がディレクトリを成しているなら、
  中の相対リンクが bare な同階層参照で外向きリンクが0であることを確かめた上で、**ディレクトリごと
  `git mv` する** —— 深さ差が全リンクに一様にかかるため、機械修正すら不要になる
  (実測: `2026-08-02-github-repos-sweep.md` の追記節2 —— 3クラスタでリンク修正0件)
- **リンク修正は移動前後の相対深さで機械的に決まる**(判断を挟まない):
  | 参照の向き | 修正 |
  |---|---|
  | 移行ファイル → 残置ファイル(旧同階層) | `<name>` → `../<旧dir>/<name>` |
  | 移行ファイル → 共移動ファイル | `../<name>`・`<旧dir>/<name>` → `<name>`(bare) |
  | 移行ファイル → その他(深さ変化分) | `../` を深さ差だけ増減 |
  | 残置 L → 移行ファイル | `<name>` → `records/<name>` または `../records/<name>` |
- 修正するのは **markdown リンク形式だけ**。地の文・インラインコードの言及は触らない
  (basename は保持されるので grep で追える)

## 3. 部分移行と保留

全数列挙の結果は「移行」または「名指し保留」の二値で閉じる(§12.1)。保留の型と条件の書き方:

- **活動中(hot)**: フェーズ完了後に再判定
- **未コミットが触れる**: ユーザーの commit 後に移行(未コミット文書の参照は壊せない)
- **クラスタ**: 正文(生きた契約)に従属する凍結 spec は、正文が凍結するまで一括で待つ
  (生きた文書から死んだ記録への非対称参照を作らないため)
- **実験プロトコルがパス参照中**: 実験(Run)完了後

保留は対象と条件を実施記録(`docs/records/`)に名指しで残す —— 次の判断が依拠できる形にする。

## 4. 検証と完了

- **リンク完全性チェック**: docs/ と README の相対 markdown リンクを全数抽出し、リンク先の存在を
  検証する(python ワンライナーで十分)。移行前から壊れていたものは別掲で記録し、直さない。
  **broken を数えたら中身を必ず見る** —— fenced code block の ASCII 図(`[ mrw serve ](トークンなし)` 等)が
  リンクとして拾われる誤検出源になる
- **git status の突き合わせ**: 変更が「rename + 意図した修正ファイル」だけであることを確認してから
  ステージする(並行セッションの変更を巻き込まない —— 明示パスで `git add`)
- **完了宣言は本流到達後**(§5.3)。幹と合流先の二層を持つリポジトリでは、どの枝への到達かを
  ブランチ名で記す

## 実例(実施記録)

- sekisho(55本判定・39本移行・保留2群・除外11本): `docs/records/2026-08-02-github-repos-sweep.md` の追記節
- 全数スイープの裁定(導入 / 保留 / 対象外の全数): 同記録の本文
- 保留4件の解除と実施(クラスタ単位の移動・`日付:` の値の採り方・移行対象0本の裁定): 同記録の追記節2
- 過去の適用: `docs/records/review.md` §12(自リポジトリ・metsuke)・§13(preference-analyzer・co-working-workflow)
