# 提案: LLMとの作業で生まれる成果物の管理ポリシー

種別: 記録 / 日付: 2026-07-25 / 状態: **導出の経緯として凍結**(規約の確定形は `policy.md`。版は同書冒頭を参照)

`issue.md` への解決案。手元のリポジトリ(metsuke / sekisho / muti-repo-workspace / claude-code-monitoring-ex / takt /
notes / priv-A / priv-B の8本、および `~/github/` 直下と `archived/`)を実測して原因を特定し、
独立レビュー計6本(第1巡: 運用の現実 / エージェント追従性 / 敵対的整合性 の3本 + 棚卸し2本、
第2巡: 自己完結性 / 敵対的整合性 / 一次資料検証 の3本。棚卸しを除き6本)の指摘を反映。
初版からの変更点と撤回した主張は `review.md` にある。

**`priv-A` / `priv-B` は非コードの個人リポジトリ(非公開)の匿名名。** 公開にあたり、内容が特定できる
リポジトリ名・ファイル名・引用を置き換えた。本数・行数・日付は実測値のまま。

> **規約の確定形は `policy.md`。本書は導出の経緯であり、規約として読まないこと。**
> 本書と `policy.md` が食い違う場合は `policy.md` が正。版ごとの変更点は `policy.md` 冒頭の変更履歴に列挙されている。第3版で変わった箇所には該当箇所に
> 「(→ 第3版で…)」の注記を入れてある(§1行で / §4.1 / §4.2 / §7.1 / §14-2 / §15。§10 は本文で言及)。

---

## 1行で

> **成果物を「種類」で分類するのをやめ、「寿命」で分類する。寿命はディレクトリ名で宣言し、既定を「消えてよい」に置く。**
> **方針が転換したときは、旧記録を消さず、該当箇所に注記する。**

2行目は新規の発明ではなく、`metsuke` の ADR 運用で既に明文化され、今日も動いている規約をそのまま採る。

**(→ 第3版で一部撤回)** 「寿命はディレクトリ名で宣言する」が当たるのは C とそれ以外の境界だけである
(パスが宣言するのは権威の有無の2値。L と A の区別はパスからは読めない。`policy.md` §2.1)。
また新設ディレクトリは `work/` と `docs/adr/` の**2つ**になった(`policy.md` §3.2)。

---

## 2. 問題の実体

### 2.1 有効な故障(実測)

| # | 故障 | 実物 |
|---|---|---|
| 3 | **リポジトリ直下への流出** | `mrw/plan.md`, `mrw/review-fb-20260716.md`, `mrw/review-codexfb-20260716.md` |
| 4 | **過程用の置き場が無い → 全部commitかuntracked litterの二択** | `mrw: rebuild-doc/`(11本) + `rebuild-doc-fb/`(8本) が丸ごと untracked のまま8〜9日放置。実測5リポジトリ中4つの `.gitignore` に過程系mdの規約がゼロ
(例外は `sekisho/.gitignore:1,13` が `RESULTS.md`/`FINDINGS.md` を committed summary と明記) |
| 5 | **リポジトリ外の孤児** | `~/github/` 直下に4本。うち2本は内容上 `muti-repo-workspace` のもの |

### 2.2 故障ではなかったもの【初版の誤診】

初版は `-fb` / `-draft` というファイル名から放置を推定したが、参照関係を確認すると**意図的に維持されている記録**だった。

```
metsuke/docs/08-dashboard.md:620        ## 17. `08-dashboard-fb.md`への対応     ← FB全項目の対応表
metsuke/docs/09-dashboard-implementation-plan.md:6   レビュー入力は 08-dashboard-fb とする
metsuke/docs/08-dashboard-fb.md:68      **結末（2026-07-25）**: …ADR 0012が…撤回した   ← 本日追記
sekisho/docs/design/README.md:57        ### synthesis-draft.md — 反映前ドラフト(透明性のため保存)
sekisho/docs/design/00-final-design-plan.md:9   > - 反映前のドラフト: synthesis-draft.md（透明性のため保存）
```

**この2件は本設計の反例ではなく、正しい運用の実例**である。§5 の昇格形態2は、これを規約化したものにほかならない。

### 2.3 3つの構造的原因

**原因1: 置き場が寿命を表明していない。**
ファイルを置いた瞬間、それが「永久に維持される文書」なのか「対応が済んだら死ぬ文書」なのかが、ファイルシステム上のどこにも書かれていない。消してよいか判断するには中身を読む必要があり、読むコストが消すメリットを上回る。

**原因2: 終端条件が定義されていない。**
中間成果物は「洗い出す」目的で作られるが、「対応が終わったら何が残るべきか」が決められていない。

**原因3: エージェントが書き先を知らない。**
規約が無ければエージェントは最も近い場所(= repo root か `docs/` 直下)に書く。
※ ただし後述 §12 のとおり、これは**推論であって実証ではない**。

---

## 3. 設計の判定基準

| # | 基準 | 根拠の強さ |
|---|---|---|
| 1 | **保守を要求する仕組みは死にやすい** | **経験則**。初版は「実測3/3が更新ゼロ」を根拠にしたが、`notes` は git 管理外で commit 単位の測定が不可能、`priv-A` は 07-10 に第2バーストがあり「更新ゼロ」ではなく「commit ゼロ」、`priv-A: <主題>-materials/` は劣化の証拠と設計妥当性の証拠に二重計上していた。実測の根拠は `priv-B` 1件に縮む |
| 2 | **判断を要求する仕組みは実行されにくい** | 経験則。ただし本提案は判断を消さない。**書き込み時から昇格時へ移す**だけである(§6) |
| 3 | **人が新しく作るファイルを増やさない** | 維持。ただし §6 の昇格は**手間を増やす**。これは意図的に払うコストとして明示する |
| 4 | **規約はエージェントに配る。人の記憶に置かない** | 維持 |

**判定質問**: 半年後に誰も手を入れなくなったとき、**正直に空になるか / 現在形で嘘をつき続けるか**。

補足: この質問に対し、実測サンプルには**空になった側の実例が1件ある**。`notes/TAXONOMY-GAPS.md` は「観点1(分類体系)の未対応項目はゼロ」に到達し、`GAPS-BACKLOG.md` は完了分を `archived/` へ複数波で移管している。索引が必ず腐るわけではない。

---

## 4. 解決案 — 寿命をパスで宣言する

### 4.1 分類軸を「種類」から「寿命」へ

**種類で分類してはいけない。** 実測では `plan / design / spec / contract / requirements / adr / decision-log / findings / review / redteam / judges / candidates / briefs / draft / progress / inventory / -fb` と**17種以上**が観測された(初版は `handoff` も数えたが、実測にあるのは
プロトコル仕様とスラッシュコマンド定義であり、成果物としての handoff 文書の実例は無かった)。種類分類は ①増える ②寿命が決まらない(`design` は生きるが `design/candidates/` は死ぬ) ③エージェントが迷い、迷ったとき root に書く、の3点で破綻する。

代わりに**寿命の3値**を第一軸にする。

| | 名前 | 書かれ方 | 終端 | 判定質問 |
|---|---|---|---|---|
| **L** | 生きる | **上書き**。常に「いまのあるべき状態」 | しない。古くなったら書き換える | 「今後も正しさを維持する責任を負う対象か?」 |
| **A** | 積む | 集合としては**追記**。個々のファイルは当時の本文を残し**注記を足す** | しない。上書きで消さない | 「**時点の記録**か? — 当時の判断と、その根拠となった発見」 |
| **C** | 消える | 一度書かれ、参照されて役目を終える | **タスク終了で終端** | 「いまのタスクのために作った道具か?」 |

**(→ 第3版で第一軸を「終端」から「権威」に置き換えた。** `work/` は定義上いかなる時点でも権威ある場所では
ないので、C が「タスク終了で権威を失う」という時点指定は観測可能な差分を持たなかった。第3版の C は
**最初から権威を持たない**。`policy.md` §2 / §2.1)

**A の定義は「時点の事実」ではなく「時点の記録(当時の判断 + その根拠となった発見)」である。** これが `issue.md` L14 の「知見」の受け皿になる。初版は「その時の解釈は昇格しない」と書いており、L14 が求めるものを消す規約になっていた。知見の大半は事実ではなく解釈だからである。

**網羅性は主張しない。** 初版は「上書きする かつ 終端する」象限が空だから3で閉じる、と論証したが誤りだった。`work/<date>-<slug>/plan.md` は複数日タスクで上書きされ、かつタスク完了で終端する。ADR も個々のファイルは上書きされる(`0011` は2回変更)。**「証明された網羅的分類」ではなく「実用的な3分類。境界に残余がある」**として扱う。残余の代表は §14 の未決2。

### 4.2 新設するのは `work/`(と、A の容器が無い場合の `docs/adr/`)

壊れているのは **C の置き場が無いこと**であり、L と A は既に機能している。
**(→ 第3版で訂正: A について不正確だった。A の容器が1つも無いリポジトリが存在し、そこでは知見が
保存できない。`policy.md` §3.2 / §5.6)**`metsuke` / `ccm-ex` の連番ナラティブ + `docs/adr/NNNN-*.md` + `{spec,contract,requirements}` 三点セットは実測中で最も規律ある構造だった。

```
<repo>/
├── docs/  README.md  src/ …    L と A。既存の規約をそのまま維持する(変更しない)
└── work/                       C。★新設(A の容器が無いリポジトリでは `docs/adr/` も。第3版)
    └── <YYYY-MM-DD>-<slug>/
```

これで**原因1が消える**: パスを見るだけで寿命が分かり、中身を読まずに消せる。
これで**原因3に対処する**(実効性は §12 で未検証と明記)。

### 4.3 既定は C

実測の散乱は例外なく「本来Cのものが L の置き場に入った」ケースである。既定をCにすると、L/A に上がるのは明示的な昇格だけになる。

**ただし判断は消えていない。書き込み時から昇格時へ移動している。** 初版は「判断が発生しない」と書いたが誤りで、昇格時の判断量はむしろ増える(§6)。これは意図的に払うコストである。

### 4.4 日付書式は `YYYY-MM-DD` に統一

実測で分裂していた(`notes/` = `YYYYMMDD_`、`priv-A/` = `YYYY-MM-DD_`)。後者に統一。ISO 8601、`ls` で正しくソート、既に多数派。

---

## 5. 方針転換の記録 — `metsuke` の ADR 規約をそのまま採る

`issue.md` L7「調査や検証をした結果として得られた発見をもとにより確度の高い方針に転換する」への答え。**実測サンプル中で唯一、方針転換を正しく扱いながら今日まで維持されている機構**がある。

```
adr/0003-no-dashboards.md:3    日付: 2026-07-17 / 状態: **部分的に置換**（2026-07-21・[ADR 0011](0011-local-dashboard.md)）
adr/0011-local-dashboard.md:3  日付: 2026-07-21 / 更新: 2026-07-25 / 状態: 採択・実装済み（S3は[ADR 0012](0012-dashboard-client-rendered.md)が撤回）
adr/0011-local-dashboard.md:8  「本文は当時の決定として残し、該当箇所に注記する」  ← 規約として明文化済み
adr/0011-local-dashboard.md:38,83 **（→ [ADR 0012](0012-dashboard-client-rendered.md) で撤回）** ← インライン注記
adr/0012-…:52                  「撤回するのは結論であって、動機ではない」
```

`0003(作らない) → 0011(部分置換) → 0012(S3のみ撤回)` の連鎖が、**本文を読むだけで追える**。

規約として採用する:

> **A の記録は上書きして書き換えない。方針が転換したら、当時の本文を残したまま、該当箇所に「(→ ◯◯ で撤回)」と注記し、冒頭の `状態:` 行を更新する。**

初版はこれを「git の上にもう一つバージョン管理を実装する行為」として却下していた。却下理由は co-working から持ち込んだものだが、そこでの理由は**複数人が書き込むと衝突する**ことであり、単独作業には当てはまらない。撤回する。

**却下を維持するのは「別ファイルとして生成される索引」だけ**(§13)。文書内の注記は、索引と違って本文と一緒にしか動かないので、剪定漏れで嘘をつくことがない。

---

## 6. 昇格 — 2形態

**昇格とは「Cのファイルを L に移すこと」ではない。**

| | 形態 | 昇格の行為 | 実例 |
|---|---|---|---|
| **1** | **L の書き換え**(既定) | 本体の該当箇所を書き換え、棄却理由を**本文に1文**入れる。C側のファイルは昇格しない | 通常のレビューFB |
| **2** | **A への凍結** | 成果物を日付付き記録として残し、**L側がそれを指して「なぜ残すか」を書く** | `metsuke: 08-dashboard.md §17` / `sekisho: README.md:57`「透明性のため保存」 |

### 6.1 どちらの形態かを決めるルール

**L の側から参照され、残す理由が書かれているものだけが A。** 逆方向(C→L)の参照は証拠にならない —— 誰でも自分から権威ある文書を指せるので、維持する意思の証拠にならない。

**この主張は第2巡レビューで撤回した。** ①「〜だけが A」は必要条件なのに十分条件として使っていた
②形態2 の定義自体が「L 側が指して理由を書く」なので、昇格時点では必ず参照0件になる循環がある
③参照が「維持する理由」かは読まないと決まらないので機械的ではない。
確定形は `policy.md` §5.1(A の**成立条件**であって判定規則ではない)。以下は導出時の検証:

| | L→C の参照 | 判定 |
|---|---|---|
| `metsuke: 08-dashboard.md §17` → fb | あり(対応表 + 理由) | **A** |
| `sekisho: 00-final-design-plan.md:9` → draft | あり(「透明性のため保存」) | **A** |
| `notes: GAPS-BACKLOG.md` / `TAXONOMY-GAPS.md` → `20260706_*.md` | **0件**(実測) | **C**(3本すべて) |

判定のタイミングは**作業直後の昇格時**であり、後からのスキャンではない。

### 6.2 種類別の昇格形

| C の種類 | 昇格 |
|---|---|
| レビューFB | 形態1が既定。L側に対応表を作って参照するなら形態2 |
| 比較検討の候補案 | 採択案をLに書き、**不採択の理由を採択案の本文に1文**。不採択案のファイルは昇格しない。高リスク設計で検討過程ごと残す判断をしたなら形態2 |
| 計画 | 実装が終われば消費済み。L側に残すのは**結果として何がそうなったか**だけ |
| 調査結果・発見 | **A へ書き写す**(当時の判断とその根拠を含めてよい)。方針転換を伴うなら §5 の注記形式で |
| 下書き | 昇格しない。final が L にある |

### 6.3 引用する。要約しない

昇格時にエージェントに要約させると、実際には言われていない理由が生成され、L 側に定着する。根拠が C 側に無ければ「(理由未記録)」と書く。

**自己検証手順**: 昇格文は C 側の原文からの**完全一致の部分文字列**とし、書き込み後に原文へ grep して一致を確認する。一致しなければ要約が混入している。

---

## 7. `work/` が解くもの・解かないもの

**解く**: 何が権威あるものかの混乱。`work/` にあるものは authoritative ではない、が保証される。

**解かない**: 削除。ここは正直に書く。

- 初版は「commit してあれば消しても履歴に残るので、消す心理的コストがゼロになる」と主張した。**これは反証済み** —— `sekisho/docs/design/synthesis-draft.md` は commit 済みで、かつ消されなかった(`00-final-design-plan.md:9` に「透明性のため保存」と明示)。撤回する
- 昇格が形態2になる成果物は、`work/` から出る動きが後から発生する(`metsuke` の FB がまさにそれ)。避けられないコストである
- **蓄積が無害だとは言わない**

### 7.1 終端の選択肢 — `archived/` への波移管

**(→ 第3版で訂正)** 「終端」と「削除」は別概念に分離された。C は `work/` にあること自体で既に
権威を持たないので、終端のために何かする必要はない。以下は**削除**の選択肢である(`policy.md` §7)。

削除以外の終端が既に実在する。`~/github/archived/` は C の墓場として機能している:

```
archived/{20260705_overall-review.md, 20260706_env-cases-expansion-plan.md,
          refactor-plan.md, GAPS-BACKLOG.md, TAXONOMY-GAPS.md, cases/}
```

`notes/GAPS-BACKLOG.md` に「対応が完了した項目は archived/ へ移管済み(**第1波** 2026-07-05 / **第2波** 2026-07-05〜07-06)」と明記されている。**定期スキャンではなく、完了時に波でまとめて動かす**運用であり、`archived/` は git 管理外なので「履歴に残す」ではなく「**権威ある場所から動かす**」パターンである。

規約: **削除は任意。終端したいときは、消すか `archived/` へ移すかを選ぶ。義務にはしない。**

義務化しない理由は、義務が果たされなかったとき、残存物が「価値があるから残っている」と誤読されるため。

### 7.2 git 前提

「commit する」「履歴に残るから消せる」は git 管理下でのみ成立する。実測で `notes` / `archived` / **この `agentic-cording-policy` リポジトリ自身**は git リポジトリではない。

規約: **git 管理下でない場所では `work/` の中身を消さない。消したいなら先に `git init` するか `archived/` へ移す。**

---

## 8. 非コードリポジトリへの一般化

3値はコードの有無に依存しない。

| repo | L(生きる) | A(積む) | C(消える) |
|---|---|---|---|
| `priv-B` | `<教材>/**`(教材本体。2粒度併存は意図的) | — | — |
| `notes` | `TAXONOMY-GAPS.md` `GAPS-BACKLOG.md`(完了分を剪定する上書き運用) | — | **`20260706_*-review.md` 3本**(L側からの参照0件) |
| `priv-A` | `<一覧>.md` | `2026-07-06_*.md`, `2026-07-10_*plan.md` | **`2026-07-10_<主題>-materials/`**(下書き3案 + 支援調査4本) |

**`<主題>-materials/` は C の典型例が既に実在している証拠**である。`-materials` という命名で既に「これは道具だ」と表明しており、7本が1タスク分としてディレクトリでまとまっている。移行は**改名だけ**で済む。

```
<any-repo>/
├── (そのリポジトリ固有の L / A)
└── work/<YYYY-MM-DD>-<slug>/      C
```

---

## 9. リポジトリ外の孤児

原因は「複数リポジトリを横断する検討に帰属先が無い」こと。

> **リポジトリに属さない検討は、`~/github/` 直下に置かず、検討専用リポジトリを作る。**

新しい発明ではない。`co-working/` と、いまこの `agentic-cording-policy/` が既にそのパターン。

ただし実測で分かった注意点: 孤児4本のうち真に帰属先が無いのは **`muti-repo-workspace-vs-takt.md` の1本だけ**。そして `devcontainer-orchestrator-architecture.md` は `mrw/docs/devcontainer-status.md:3` と `docs/devcontainer-status.ja.md:3` の2本から相対リンクで**実際に参照されている** —— 孤児に見えて生きた依存であり、移動するならリンク修正とセットでなければ docs が壊れる。

---

## 10. 実装 — CLAUDE.md スニペット

**確定形は `policy.md` §11 にある。本書には転記しない。**
複製すると本体の改訂に追随せず古い規約を配ることになる —— 実際、第2版まで本書と `policy.md` に
別々のスニペットが並存し、形態2 の宛先(「`work/` に置いたまま」vs「`work/` の外へ移す」)と
A の容器の扱い(「新設せず `work/` へ」vs「`docs/adr/` を新設」)が**正反対**になっていた。
第3版でこの重複を解消した。

初版のスニペットは、独立レビューの18入力例中**14例で「迷う」または「自信を持って誤着地」**した。
最大の原因は **A(積む)がスニペットに一文も無かった**こと。第2版で A・命名規則・引用の自己検証・
非文書成果物・並列衝突を追加し、第3版で昇格の宛先・完了条件・版表記を追加している。

---

## 11. 作らないもの

| 作らないもの | 理由 | 根拠の強さ |
|---|---|---|
| **別ファイルとして生成される索引**(`INDEX.md` / `timeline.md`) | 剪定されないまま現在形で嘘をつき、嘘だと検知できない。`ls work/` で足りる | ただし `notes/TAXONOMY-GAPS.md` は剪定されて未対応項目がゼロに到達した実例。**絶対視しない** |
| 削除の義務化 | 義務が果たされないとき、残存物が「価値がある」と誤読される | 維持 |
| 文書種類の細分類タクソノミー | 増える。エージェントが迷う(原因3の再生産) | 実測17種。**崩せなかった** |
| `work/` の gitignore | untracked litter になる(`rebuild-doc/` が実測で放置された) | 維持 |
| 定期棚卸し lint / cron | 保守を要求する | **未検証の懸念**。サンプルに lint/cron の実例は0件で「実証済み」とは言えない |

**却下から外したもの**: 文書内の `状態:` 注記 / supersede 注記 / ID採番。初版は却下していたが、`metsuke` の ADR で機能しており、`issue.md` L7 への回答を供給する(§5)。

---

## 12. 未検証の前提【明示】

| 前提 | 状態 |
|---|---|
| **スニペット1枚で root 流出が止まる** | **未検証**。`mrw/CLAUDE.md:5,32-41` には既に配置規約が存在する(`tasks/` は生成状態・commit しない等)。それでも `review-fb-*.md` `plan.md` `rebuild-doc*/` が root に生成された。既存規約は md 成果物を対象にしていないので直接の反証ではないが、支持もされていない |
| 昇格の粒度 | `metsuke` は18項目(+軽微1行)をID単位で対応表化した。「1文書き足す」で足りるのか対応表を作るべきか、基準が無い |
| `work/` の削除契機 | 存在しない。`archived/` への波移管が唯一の実例 |

したがって §15 の「次の一歩」は、全リポジトリへの一斉配布ではなく**1リポジトリでの先行適用と観測**にする。

---

## 13. 実測の故障との対応

| 故障 | 帰結 | 検証 |
|---|---|---|
| 3. root流出 | 既定が `work/` なので、迷ったときの着地点が root ではなくなる | **未検証**(§12) |
| 4. 過程用の置き場が無い | `work/` が置き場。commit するので untracked litter にもならない | 構造上成立 |
| 5. リポジトリ外の孤児 | 横断検討は検討専用リポジトリを作る | 反証されなかった |
| (L7 方針転換) | A の記録に `状態:` 注記と supersede 注記を入れる | `metsuke` で実証済み |
| (L14 知見) | A の定義を「時点の記録(当時の判断 + 根拠となった発見)」に広げる | `metsuke: 0012:52` が実例 |

---

## 14. 未決

| # | 論点 | 現案 |
|---|---|---|
| 1 | `work/` の名前 | `work/`。`.work/`(dotted)は**採らない** —— 見えないと蓄積が見えず「正直な劣化」が成立しない |
| 2 | `findings` の A/C 判別 | **(→ 第3版で訂正: 機械的には決まらない。`policy.md` §5.1)** 参照ルールを当てても、`sekisho: p0〜p4-findings.md` のように**事実と当時の解釈が同一ファイルに混在**する場合、ファイル単位でしか判定できない。3分類の残余 |
| 3 | 昇格の粒度 | §12 参照。1文か対応表かの基準 |
| 4 | 既存リポジトリへの適用 | 今後のみ。棚卸し表は `inventory.md` に出すが移行は実行しない。ただし `inventory.md` 自身は生成一覧なので `work/2026-07-25-artifact-policy/` に置いて C とする |
| 5 | **L→C 参照ルールの限界** | 棚卸しで実例が1件出た。`mrw/plan.md` は `docs/mrw-cli.md` 他3本から「現状の実装状況源」として参照されており、ルール上は **A** になるが、内容は消費済みの計画に見える。**参照は「維持する意思」の代理指標であって、参照元が単に更新されていないだけの場合を区別できない。** 誤る方向は「Cを A と判定する」= 安全側(消さない)なので実害は小さいが、限界として明記する |

---

## 15. 次の一歩

**§14 の未決5点は 2026-07-25 に確定済み**(`work/` の名前 = `work/` / findings は分離せずファイル単位 /
昇格の粒度は既定1文・対応表は任意 / 既存散乱は移行しない / 参照ルールの限界は明記)。
`policy.md` として制定済み。以下は制定後の手順。

1. ~~未決5点を確定して `policy.md` に落とす~~ **完了(2026-07-25)**
2. **`muti-repo-workspace` で先行適用**し、観測する。全展開はその後

   **(→ 第3版で対象を `metsuke` に変更。** `mrw` は 2026-07-17 以降リポジトリ全体が停止しており、
   新しい作業が発生しないので観測が成立しない。`metsuke` も `docs/` 直下への流出実績があり、かつ
   稼働中である。流出の形は違う —— mrw は root、metsuke は `docs/` 直下 —— がどちらも権威ある場所への
   混入である。`policy.md` §12)

   対象を `mrw` にする理由: root 流出が実際に起きた**唯一のサンプル**であり、かつ
   `CLAUDE.md:5,32-41` に既に配置規約が存在する。つまり §12 の未検証前提
   「スニペット1枚で着地点が変わる」を**判別できる唯一の条件**が揃っている。

   観測項目: 新しく生成される C 性の成果物が、root ではなく `work/` に着地するか。

   ※ `priv-A/<主題>-materials/`(改名のみ・7本)は**最も低コストな移行例**だが、
   `docs/` も root 流出の履歴も無いため、先行適用の対象としては何も検証できない。移行例と
   パイロットは分けて扱う。
3. `CLAUDE.md` スニペットの配布(実装は Codex)
4. `inventory.md` は `work/2026-07-25-artifact-policy/` に置く。移行は実行しない
