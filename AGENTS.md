# AGENTS.md

## 作業成果物の置き場

このリポジトリは成果物管理ポリシーの本体を置く検討専用リポジトリである。
**規約は同居する [policy.md](policy.md) が正**(現行版は同書冒頭の版表記を参照)。
ここには §8「リポジトリに属さない検討」に該当する差分だけを書く —— スニペット(§11)は
他リポジトリへ配る複製であり、本体が同居するこのリポジトリには複製を置かない。

§8 の構成:

- **権威ある場所はリポジトリ直下**。`docs/` も `docs/adr/` も作らない(§8。§3.2 の新設規定は
  既存の規約が無いこのリポジトリには適用されない)
- **C は `work/<YYYY-MM-DD>-<slug>/`**。既定はここ。迷ったらここ(§3)
- **直下の L / A の判別は §2.1 の規則で行う** —— 冒頭に `種別: 記録` を持つ文書は A。
  上書きせず §6 の方式で注記する。持たないものは L で、直接上書きしてよい
- 新しい A を直下に作るときは、ファイル名を主題の名詞句とし(`work/` の日付プレフィクスは
  使わない)、冒頭に `種別: 記録` と `日付: / 更新: / 状態:` 行を置く(§8)
- 例外: 外部ツールがパスを固定する成果物(`docs/cowork/<task-id>/intent.md` 等)は
  指定パスに置き、記入して commit する(§3.3)

命名・昇格・引用の規律・方針転換の記録は policy.md §4 / §5 / §5.4 / §6 に従う。
版を改訂するときは §11 の版の運用規則(発行は master 上の policy.md に限る / 改訂・タグ付与・
配布は同一の流れ)に従う。
