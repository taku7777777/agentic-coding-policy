# AGENTS.md

## 作業成果物の置き場

このリポジトリは成果物管理ポリシーの本体を置く検討専用リポジトリである。
**規約は同居する [policy.md](policy.md) が正**(現行版は同書冒頭の版表記を参照)。
スニペット(§11)は他リポジトリへ配る複製であり、本体が同居するこのリポジトリには複製を置かない。
ここには §8「リポジトリに属さない検討」の差分だけを書く。

- **分類はパスで決まる**(§2.1): `docs/records/**` = A(時点の記録。上書きせず §6 の注記方式)/
  `work/**` = C(権威なし)/ それ以外 = L(直接上書きしてよい)。
  冒頭に `種別: 記録` を持つ文書も A として扱う(レガシー互換。**新規に書かない**)
- **L はリポジトリ直下**(README.md / policy.md / AGENTS.md / LICENSE)
- **A は `docs/records/`**。issue.md / proposal.md / review.md は移行済みの既存レコードで
  名詞句名のまま。新しい記録は `<YYYY-MM-DD>-<slug>.md`(§4)
- **C は `work/<YYYY-MM-DD>-<slug>/`**。既定はここ。迷ったらここ(§3)
- `docs/adr/` は作らない(特別容器の新設は廃止。決定も records/ へ —— §5.6)
- 例外: 外部ツールがパスを固定する成果物(`docs/cowork/<task-id>/intent.md` 等)は
  指定パスに置き、記入して commit する(§3.3)

命名・昇格・引用の規律・方針転換の記録は policy.md §4 / §5 / §5.4 / §6 に従う。
版を改訂するときは §11 の版の運用規則(発行は master 上の policy.md に限る / 改訂・タグ付与・
配布は同一の流れ)と §5.3(**完了の宣言は本流到達後**)に従う。
