# 成果物管理ポリシー

LLMとの作業で生まれる成果物の置き場・権威・寿命の規約。本書だけで運用できる。

日付: 2026-07-25 / 更新: 2026-07-26(第5版) / 状態: **運用可。未検証の前提あり(§10)**

第5版の主な変更(第4巡レビュー反映): 形態2 の手順・完了条件に `種別: 記録` マーカーの付与を追加(§5.3・§11。マーカーが無いと §2.1 の判別で昇格直後の A が L と誤判定され上書きされるため)/ 既知名以外の新設容器にもマーカーを必須化(§5.6)/ ツールがパスを固定する成果物の例外を新設(§3.3)/ §12 の観測期限を制定日起点から**配布日起点**に変更し、git 監査に昇格・流出の分類手順と `-uall` を追加(§12)。指摘と裁定は `review.md` §10。

第4版の主な変更(第3巡レビュー反映): L/A の判別子を「`状態:` 行の有無」から「A の容器 or `種別: 記録` マーカー」へ置き換え(§2.1)/ スニペットの配布先を `AGENTS.md` 正 + `CLAUDE.md` は `@AGENTS.md` に変更(§11)/ §12 に観測の計装・観測期間・昇格リマインドを追加 / 引用の照合を行単位に限定(§5.4)。指摘と裁定は `review.md` §9。

第3版の主な変更: 3分類の第一軸を「終端」から**「権威」**に置き換え(§2)/ 昇格形態2 の宛先を
`work/` の外と規定(§5)/ §5.1 の適用範囲を2つに限定(§5.1)/ 知見を形態2 の目安に追加(§5)。
検討の経緯は `proposal.md`、レビューと裁定は `review.md`。

**本書は L(§8)。** 改訂は上書きで行う。A の容器の中になく、冒頭に `種別: 記録` を持たないため、
§2.1 の判別規則でも L となる。

---

## 1. 目的と適用範囲

**目的**: LLMと作業を進める過程で生まれる成果物(主に md)を、人手の保守を要求せずに管理する。

**適用範囲**: コードリポジトリ、非コードの個人リポジトリ、リポジトリに属さない横断検討のすべて。

**適用時期**: 本ポリシー制定以降の新規作業。既存の散乱は移行しない。

### 用語

| 語 | 意味 |
|---|---|
| **権威ある場所** | `work/` 以外。そこにある文書は「読んで信じてよいもの」として扱われる |
| **権威** | 現在形の権威(L: いまこうである)と、当時形の権威(A: 当時こう判断した)がある。§2 |
| **L / A / C** | §2 の3分類。「L側」= L に分類される文書、の略記 |
| **昇格** | `work/` の成果を権威ある場所に反映すること。§5 |
| **A の容器** | A を積む先。`docs/adr/`, `CHANGELOG.md`, `docs/design/01-decision-log.md` など |
| **`種別: 記録`** | A 文書の冒頭マーカー。A の容器の**外**にある A 文書はこれを冒頭メタ行に持つ(§2.1) |
| **作業メモ** | 単一タスクに属さない日々のメモ。直近のタスクディレクトリに書く。該当が無ければ `work/<YYYY-MM-DD>-notes/` |

---

## 2. 3分類 — 第一軸は権威

| | 名前 | 権威 | 書かれ方 | 判定質問 |
|---|---|---|---|---|
| **L** | 生きる | **現在形**。「いまこうである」として読んでよい | **上書き**。古くなったら書き換える | 「今後も正しさを維持する責任を負う対象か?」 |
| **A** | 積む | **当時形**。「当時こう判断した」として読んでよい。現在も有効かは `状態:` 行が示す(§6) | 集合としては**追記**。個々のファイルは当時の本文を残し**注記を足す** | 「**時点の記録**か? — 当時の判断と、その根拠となった発見」 |
| **C** | 消える | **無い**。`work/` にあること自体が「信じる対象ではない」の宣言 | 一度書かれ、参照されて役目を終える | 「いまのタスクのために作った道具か?」 |

**A は事実だけでなく解釈を含む。** 「時点の事実」ではなく「時点の記録 = 当時の判断 + その根拠となった発見」である。
再利用可能な知見の大半は事実ではなく解釈であり、A がその受け皿になる。

### 2.1 この分類について正直に書いておくこと

- **C は「終端する」のではなく、最初から権威を持たない。** 第2版は C を「タスク終了で終端」としていたが、
  `work/` は定義上いかなる時点でも権威ある場所ではないので、持ったことのない権威は失えない。
  時点指定は観測可能な差分を持たなかったため撤回した
- **網羅的ではない。** 「上書きされ、かつタスク限りで役目を終える」ものが存在する(複数日タスクの
  `plan.md` など)。これは C に倒す。分類の正しさではなく便宜である
- **「3」であることに論拠は無い。** 実用上この3語で足りているという以上の主張はしない
- **パスが宣言するのは権威の有無(2値)だけである。** `work/` = 権威なし、それ以外 = 権威あり。
  **L と A の区別はパスでは分からない**(同じ場所に同居する)。両者を分けるのは所在(A の容器の中か)または書き方(`種別: 記録` マーカー)であり、
  判別規則は本節末尾に定める

**既存文書を更新するときの判別**: 次のいずれかに当てはまる文書が **A** —— 上書きせず §6 に従って注記する。

1. 既知の A の容器(`docs/adr/**`, `CHANGELOG.md`, decision log 等。§1)の中にある
2. 冒頭メタ行に `種別: 記録` を持つ

どちらにも当てはまらなければ **L** —— 直接上書きしてよい。

**`状態:` 行は判別に使わない。** `状態:` は A の記録の現在の有効性を示す行であり(§6)、L の文書が成熟度の表示のために持つこともある(本書自身が例)。第3版は「`状態:` 行の有無」を唯一の判別子としていたが、本書(`状態:` 行を持つ L)を A と誤判定し、`CHANGELOG.md`(`状態:` 行を持たない A の容器)を L と誤判定する。両方向に誤るため第4版で置き換えた。

---

## 3. 置き場

```
<repo>/
├── docs/  README.md  src/ …     権威ある場所。L と A が同居する
│   └── adr/                     A の既定の容器(§5.6)
└── work/                        権威なし。C だけ
    └── <YYYY-MM-DD>-<slug>/
```

**既定は `work/`。** 次の2つに**明確に**当てはまるときだけ、権威ある場所へ書く。当てはまるか迷ったら `work/`。

1. 今後も正しさを維持する責任を負う → L。該当文書を直接書き換える
2. 時点の記録(当時の判断と、その根拠になった発見) → A。既存の A の容器に追記する。無ければ §5.6

**文書以外も同じ。** 使い捨てスクリプト・実行ログ・生成データ・画像も `work/<YYYY-MM-DD>-<slug>/` 配下。

### 3.1 不変条件 — 権威ある場所は `work/` に依存しない

> **権威ある場所の文書は、`work/` の中身に**依存**してはならない。**
> `work/` を丸ごと消しても、権威ある場所の文書の主張が成立しなくなってはいけない。

「依存」とは、その文書を読んで内容を理解したり主張の根拠を確かめたりするために `work/` を
読む必要がある状態を指す。**単なる所在の言及(「〜は `work/` にある」)は依存ではない**
—— リンクが切れるだけで、主張は壊れない。

残す価値があると判断した成果物は §5 形態2 で **`work/` の外へ出す**。これにより
「`work/` は読まずに消せる」が成立する。**ただし無条件ではない**(§7.1)。

### 3.2 新設してよいもの

| | 条件 |
|---|---|
| `work/` | 1リポジトリ1つ |
| `docs/adr/`(A の容器) | A の容器が1つも無い場合のみ、1リポジトリ1回。§5.6 |

それ以外の置き場は既存の規約を変更しない。検討専用リポジトリは §8。

(`proposal.md` は「新設は `work/` 1つだけ」を売りにしていたが、A の容器が無いリポジトリが
存在する以上それでは知見が保存できないため、第3版で2つに改めた。同時に、`proposal.md` §4.2 の
「L と A は既に機能している」という診断は**A について不正確**だったことになる)

**`work/README.md` は置かない。** 「`work/` がある限り正しくあり続けねばならない」= L でありながら
パスは権威なしという自己矛盾になる。寿命の宣言は §11 のスニペットが担う。

### 3.3 例外 — ツールがパスを固定する成果物

外部ツールが読み取りパスを固定している成果物は、そのパスが権威ある場所の中でも、指定パスに置く。
実例: cowork の `docs/cowork/<task-id>/intent.md`(ツールはフックのたびにこのパスだけを読む。
移動はコード変更なしにできない)。ただし:

- **未記入テンプレートのまま放置しない。** 置いたら記入し、commit する(untracked 放置は実測故障 #4 の再演)
- **タスク完了時に冒頭へ `種別: 記録` を付す。** 以後は当時の方針の記録(A)として §6 で扱う
- この例外は「ツールがパスを固定している」場合に限る。人が置き場を選べる成果物には適用しない

---

## 4. 命名

- 日付書式は **`YYYY-MM-DD`**(ISO 8601)。`YYYYMMDD` は使わない
- `<YYYY-MM-DD>` は**そのタスクで最初に `work/` に書いた日**。タスクが続く限り変えない。
  **今日の日付ではない。** 3日前に始めたタスクの続きなら3日前の日付のディレクトリに追記する
- 着手時に `ls -t work/` を見て、同じ主題の既存ディレクトリがあればそこに追記する。
  新規作成は該当が無い場合のみ
- `<slug>` は英数字とハイフン、2〜5語の名詞句(`dashboard-review`, `api-latency-bench`)
- ファイル名は `<内容>.md`。並列・複数観点のときは `<担当>-<内容>.md` で一意にし、互いに上書きしない
- 複数リポジトリに跨るタスクでは、`work/` は**作業ディレクトリのリポジトリにだけ**作る
- `work/` 内で経緯や気づきを書き足すときは、既存ファイルに追記する。該当が無ければ `notes.md`
- 権威ある場所へ出すときの命名は、**そのリポジトリの既存規約に従う**(`docs/adr/NNNN-<slug>.md`、
  連番ナラティブ `NN-<slug>.md` など)。`work/` の命名規則は持ち込まない

---

## 5. 昇格

**昇格とは「C のファイルを L に移すこと」ではない。** 2つの形態がある。

| | 形態 | 行為 | `work/` 側 |
|---|---|---|---|
| **1** | **L の書き換え**(既定) | 本体の該当箇所を書き換え、棄却理由を**本文に1文**入れる | 残る。権威は持たない |
| **2** | **権威ある場所へ出す** | 成果物を **`work/` の外へ移し**、権威ある場所の文書がそれを指して「なぜ残すか」を書く。移した先の冒頭に `種別: 記録` を付す(§5.3) | **無くなる**(移動する) |

**どちらを選ぶかは、昇格を行う人またはエージェントが決める。** 以下は目安であり、規約が決定するのは
「迷ったら形態1」ということだけである。

形態2 を選ぶ目安:

- レビューの各指摘にどう対応したかを後から追う必要があるとき
- 高リスク設計で検討過程ごと残すと決めたとき
- **再利用可能な知見・発見を含み、1文に圧縮すると失われるとき**(`issue.md` L14 の中心。
  第2版はこれを目安に入れておらず、知見が既定で形態1 に流れて失われていた)

**形態2 の宛先は「権威ある場所の適切な位置」であり、必ずしも A の容器の中ではない。**
実測の2例 —— `metsuke: docs/08-dashboard-fb.md`、`sekisho: docs/design/synthesis-draft.md` ——
はどちらも `docs/` 配下だが `docs/adr/` の中ではない。既存の docs 構造に素直に置けばよい。

### 5.1 昇格が完了したことの検証条件

> **形態2 が完了しているとは、出した先が権威ある場所にあり、かつ権威ある場所の別の文書から
> 「維持する理由」を付して参照されている状態を指す。**

**これは A 一般の定義ではない。** 適用範囲は次の2つに限る。

| 用途 | 位置づけ |
|---|---|
| (a) **形態2 の完了検証** | 規範。参照文を書き忘れたら形態2 は未完了 |
| (b) **既存ファイルの遡及調査** | **手がかりであって判定ではない。** 権威ある場所にあるものがまだ維持されているかを調べる補助。最終判断は中身を読んで下す |

権威ある場所に**直接書かれた** A(新設した ADR など)には適用しない。被参照0件でも A である。

**「維持する理由」とは何か。** 参照元の内容がその文書に依存している関係が明示されていること
—— すなわち、参照先が変われば参照元も変わるべきだと読める形になっていること。

| 該当する | 該当しない |
|---|---|
| 役割の明示: `出典:` `上位:` `根拠:` `レビュー入力:` | 所在の案内: 「〜にある」「〜に保存」 |
| 保存理由の明記: 「透明性のため保存」 | 参照文が対象を「中間成果物」「作業用」と性格づけている |
| 文書表に目的・内容を付して掲載している | ファイル名を列挙しただけ |

**この判別は機械的ではない。** 参照の有無は `grep` で分かるが、それが依存関係の明示かどうかは
読まないと決まらない。用途(b)で調べるときは、**検索範囲を権威ある場所の全体にすること**
(`docs/` 直下だけでなく `docs/**` すべて)。この範囲を誤って5本を誤判定した実例がある。

### 5.2 粒度

**既定は1文。** 本体の該当箇所を書き換え、棄却理由を1文入れれば足りる。

指摘を ID 単位で対応表にすることは**任意**。これは基準ではなく基準の不在であり、意図的にそうしている
—— 常時の義務にすると、継続できなくなった瞬間に規約全体が崩れるため。

### 5.3 完了条件

| 形態 | 完了条件 |
|---|---|
| 1 | 権威ある場所のファイルが変更されたこと。`work/` 側への変更・削除は含めない |
| 2 | 成果物が `work/` の外へ移動し、冒頭に `種別: 記録` と `日付: / 状態:` 行が付され(移動先が A の容器の中である場合は不要)、**かつ** §5.1 の参照文が書かれたこと。**すべて**が必要 |

マーカーを付すのは §2.1 の判別を成立させるためである。形態2 で出した文書は A の容器の外に置かれることがあり(§5 の実測2例はどちらも容器の外)、マーカーが無いと次の更新で L と判定され、当時の記録が上書きされる。第4版まではこの手順が欠けていた(`review.md` §10)。

最後に、変更したファイルのパスを**作業の応答内で1行報告する**(新しいファイルは作らない)。

### 5.4 引用する。要約しない

要約させると、実際には言われていない理由が生成され、権威ある場所に定着する。

- **原文にある記述を昇格するとき**(指摘の内容、実測値、発言)は、原文からの**完全一致の部分文字列**とし、**改行を含まない単位**(原文の1行に収まる範囲)で引用する。書き込み後、原文へ grep して一致を確認する。複数行にわたる内容を残したいときは、引用を行ごとに分けるか、形態2(ファイルごと出す)を使う(md の折り返しを跨ぐ文字列は行単位の grep で照合できないため)
- **昇格時に新しく生成される判断**(なぜその指摘を採らないか等)は原文に存在しないので、この規律の対象外。
  ただし**その判断が自分のものであることが分かるように書く**(「(本対応時の判断)」等)
- 原文にあるはずの根拠が見つからなければ「(理由未記録)」と書く

**この手続きは出所を確認するだけで、引用が適切であることは保証しない。** 無関係な一致でも通る。
検出できるのは「要約が混入した」場合だけである。

### 5.5 昇格の例(規約ではなく例示)

**これは分類のタクソノミーではない。** 置き場の分類は §2 の3分類だけであり、以下は §5 の適用例。
ここに無い種類が出てきたら §3 の既定(= `work/`)と §5 の目安に立ち返る。

| 例 | 昇格 |
|---|---|
| レビューFB | 形態1が既定 |
| 比較検討の候補案 | 採択案を L に書き、**不採択の理由を採択案の本文に1文**。不採択案のファイルは昇格しない |
| 計画 | 実装が終われば消費済み。L に残すのは**結果として何がそうなったか**だけ |
| 調査結果・発見 | **形態2**(§5 の目安3)。1文に圧縮すると知見が失われるため |
| 下書き | 昇格しない。final が L にある |
| 事実と解釈が混在する findings | **分離しない。ファイル単位で扱う** |

### 5.6 A の容器が無い場合

A の容器(`docs/adr/`, `CHANGELOG.md` 等)が存在しないリポジトリでは、**`docs/adr/NNNN-<slug>.md` を新設する。**

「人が作るファイルを増やさない」は**毎回増やさない**という意味であって、**一度も作らない**ではない。
容器が無いまま知見を `work/` に書くと、それは権威を持たない側に残る。それでは知見を残す目的を満たせない。

- **`docs/adr/` が既定なのは「決定・知見」を積む場合。** 内容に合う容器が別にあるならそちらを作る
  (変更点の時系列なら `CHANGELOG.md`、実測値の蓄積なら `docs/BENCH.md` など)。
  ADR 形式を性質の違う記録に無理に当てはめない
- 既知名(`docs/adr/`・`CHANGELOG.md`)以外の名前で容器を新設するとき(`docs/BENCH.md` 等)は、冒頭メタ行に `種別: 記録` も置く。§2.1 の判別1 が効くのは既知名だけであり、マーカーが無いとリポジトリ固有の容器は L と誤判定されるため
- 書式: 冒頭に §6 の `日付: / 更新: / 状態:` 行。本文は「決定 / 背景 / 根拠となった発見 / 影響」
- 連番 `NNNN` は既存の最大値+1。並列で昇格するときは採番を1エージェントに絞る
- 1リポジトリにつき一度作れば以後は追記だけになる
- 検討専用リポジトリでは `docs/adr/` を作らず、リポジトリ直下に A を置く(§8)

---

## 6. 方針転換の記録

> **A の記録は上書きして書き換えない。方針が転換したら、当時の本文を残したまま、
> 該当箇所に「(→ ◯◯ で撤回)」と注記し、冒頭の `状態:` 行を更新する。**

`metsuke/docs/adr/0011-local-dashboard.md:8` に「本文は当時の決定として残し、該当箇所に注記する」と
既に明文化され、`0003(作らない) → 0011(部分置換) → 0012(S3のみ撤回)` の連鎖が本文を読むだけで追える。

```
日付: 2026-07-17 / 状態: **部分的に置換**（2026-07-21・[ADR 0011](0011-local-dashboard.md)）
日付: 2026-07-21 / 更新: 2026-07-25 / 状態: 採択・実装済み（S3は[ADR 0012](…)が撤回）

本文中の該当箇所:  **（→ [ADR 0012](…) で撤回）**
```

**結論は撤回するが、その結論に至った根拠は消さない。** 発見(= 知見)は当時の記録として残す。

**`状態:` 行が、その A の現在の有効性を示す唯一の場所である。** A の権威は当時形なので
(§2)、本文をそのまま「いま有効」と読んではならない。有効性は `状態:` 行で確認する。

**まだ A が存在しない段階(`work/` で作業中)で方針が変わったとき**は、`work/` の中で経緯を書き足せばよい。
注記が必要になるのは権威ある場所へ出して以降である。

---

## 7. 削除

**権威の有無(§2)と削除(ファイルを消すこと)は別である。** C は `work/` にあることで既に
「信じる対象ではない」と宣言されており、これは**何もしなくても成立している**。

そのうえで、**削除は任意。義務にしない。** 義務が果たされなかったとき、残存物が
「価値があるから残っている」と誤読されるため。

| 方法 | 条件 |
|---|---|
| **何もしない** | **既定**。`work/<YYYY-MM-DD>-<slug>/` は日付付きなので `ls work/` がそのまま索引になる |
| 削除する | §7.1 の条件を満たす場合 |
| `archived/` へ移す | git 管理外でも可。完了時に**まとめて**動かす(定期スキャンはしない) |

### 7.1 「読まずに消せる」が成立する条件

§3.1 の不変条件は「読まずに消せる」を目指すが、**無条件ではない**。次の3つを満たすときに限る。

1. **git 管理下であること。** 管理外なら唯一のコピーが消える(§7.2)
2. **そのタスクが完了していること。** 継続中のタスクのディレクトリかどうかは**パスから分からない**
   (§4 は3日前のディレクトリへの追記を認めている)
3. **昇格が実施済みであること。** 未昇格なら `work/` が唯一のコピーである。
   §10-5 のとおり、昇格の未実施は本ポリシー自身が最も起こると予測している故障モードである

3条件のうち2と3はパスに書かれていないので、**「読まずに」消せるのは自分が完了させたタスクだけ**である。

### 7.2 git 前提

**git 管理下でない場所では `work/` の中身を消さない。** 消したいなら先に `git init` するか `archived/` へ移す。

**`work/` は commit する。理由は「失われないため」である。**
gitignore すると、そのタスクで得た記録がバックアップも共有もされないまま消えうる。
(第2版はこれを「gitignore すると untracked litter になる」と説明していたが誤り。gitignore すれば
定義上 litter ではなくなる。実測の放置事例は gitignore されていなかったので、gitignore の害の証拠でもない)

**`work/` は蓄積する。** 本ポリシーが解くのは「何が権威あるものか」の混乱であって、蓄積そのものではない。
蓄積が無害だとは主張しない(§10-4)。

---

## 8. リポジトリに属さない検討

> **リポジトリに属さない検討は、リポジトリの親ディレクトリに置かず、検討専用リポジトリを作る。**

ここでは §3.2 の「新設は `work/` と `docs/adr/`」は適用されない(既存の規約が存在しないため)。
権威ある場所をリポジトリ直下に置く。

```
<検討専用repo>/
├── issue.md  policy.md                   L(現在形の権威)
├── proposal.md  review.md                A(当時形の権威。冒頭に `種別: 記録`)
└── work/<YYYY-MM-DD>-<slug>/             C
```

直下に置く A は、冒頭に `種別: 記録` のマーカー(§2.1。容器の外なのでパスからは A と分からない)と、§6 の `日付: / 更新: / 状態:` 行を持つ。ファイル名は主題を表す名詞句とし、
`work/` の命名規則(日付プレフィクス)は使わない。

移動する前に、そのファイルが他所から参照されていないか確認する
(孤児に見えて生きた依存であることがある)。

---

## 9. 作らないもの

| 作らないもの | 理由 | 根拠の強さ |
|---|---|---|
| 文書種類の細分類タクソノミー | 増える。エージェントが迷い、迷ったとき権威ある場所に書く | **強い**。実測17種。反証されなかった |
| 削除の義務化 | 義務が果たされないとき、残存物が「価値がある」と誤読される | 中。§7 の設計と一貫 |
| **生成される**索引(`INDEX.md` / `timeline.md`) | 剪定されないまま現在形で嘘をつき、嘘だと検知できない | **弱い**。剪定されて未対応項目ゼロに到達した反例がある。絶対視しない |
| `work/` の gitignore | 記録が失われる(§7.2) | 中 |
| 定期棚卸し lint / cron | 保守を要求する | **弱い**。実測サンプルに lint/cron の実例は0件で「実証済み」とは言えない |

**手で維持する README の文書表は対象外**(むしろ §5.1 の参照文の受け皿になる)。
ただしこれも腐る。腐れば §5.1 用途(b)の手がかりも狂う —— だから用途(b)は判定ではなく手がかりに留めてある。

**§10-2 の誤 A を許容しつつ索引を禁じるのは非対称に見えるが、害の及ぶ範囲が違う。**
誤 A は1つの成果物についての主張であり、その文書を読めば嘘だと分かる。索引は集合全体についての
主張であり、**何が抜けているかは読んでも分からない**。検知可能性が違う。

**禁止しないもの**: 文書内の `状態:` 注記 / supersede 注記(§6)。索引と違い本文と一緒にしか動かないので、
剪定漏れで嘘をつかない。

---

## 10. 既知の限界と未検証の前提

**「限界として開示したから免責される」わけではない**ので、それぞれ何に該当するかを明示する。

| # | 事項 | 分類 | 内容 |
|---|---|---|---|
| 1 | スニペット1枚で置き場が変わる | **前提(不成立なら設計が無効)** | §11 が「実効性の本体」と呼ぶものが未検証。`muti-repo-workspace` には既に配置規約があるのに流出が起きた。→ §12 |
| 2 | §5.1 用途(b)の精度 | **規則の弱さ** | 参照は「維持する意思」の代理指標であり、参照元が単に更新されていないだけの場合を区別できない(`mrw/plan.md` が実例)。害は「消さないこと」ではなく、権威ある場所に偽の「なぜ残すか」が定着すること。ゆえに用途(b)は判定ではなく手がかりに留める(§5.1) |
| 3 | 3分類の残余 | **真の限界** | 「上書きされ、かつタスク限りで役目を終える」ものは C に倒す。C は消さなくてよいので実害は生じない |
| 4 | 蓄積は解かない | **適用範囲の縮小** | `issue.md` の「管理しづらい」は (i) 権威ある場所への混入 と (ii) 蓄積 の2成分あり、本ポリシーは (i) だけを解く |
| 5 | 昇格の手間 | **設計自身が予測する故障モード** | 昇格は書き込み時より判断も手間も多い。判断を消したのではなく書き込み時から昇格時へ**移した**。最初に破られるのはここである |
| 6 | **知見の保存は昇格に依存する** | **#5 の帰結** | `issue.md` L14 について本ポリシーが用意したのは**経路**であって保証ではない。昇格が実行されなければ知見は `work/` に残り、権威を持たないままになる。「L14 を解決した」とは言わない。経路が使われるかは §12 で観測する |

---

## 11. 配布用 AGENTS.md スニペット

各リポジトリの **`AGENTS.md`** に貼り、`CLAUDE.md` には `@AGENTS.md` の1行を置く。これが実効性の本体。
`AGENTS.md` を正とするのは、実装を委譲している Codex が `CLAUDE.md` を読まないため(Codex が読むのは
`AGENTS.md`。Claude Code は `CLAUDE.md` の `@AGENTS.md` import で同じ内容を読む)。実体は1リポジトリ1枚に保たれる。

**スニペットは本書からの複製である。** 本書が改訂されると配布済みのコピーは古くなる ——
これは §9 が索引を禁じる理由と同じ構造を持つ。したがって**先頭に版を書き、古いことを検知可能にする**。
検知できない嘘は作らない、が §9 の趣旨であり、版付きの複製はその条件を満たす。

```markdown
<!-- artifact-policy v5 (2026-07-26) — 本体: agentic-cording-policy/policy.md -->
## 作業成果物の置き場

**既定は `work/<YYYY-MM-DD>-<slug>/`。** 迷ったらここ。
対象: 調査結果 / レビューFB / 比較検討の候補案 / 下書き / 計画 / 作業メモ / 引き継ぎメモ、
および文書以外の中間物(使い捨てスクリプト・実行ログ・生成データ・画像)。

次の2つに**明確に**当てはまるときだけ、`work/` の外(権威ある場所)へ書く:
1. 今後も正しさを維持する責任を負う → `docs/` 等の該当文書を直接書き換える
2. 時点の記録(当時の判断と、その根拠になった発見) → 既存の追記型ファイル
   (`docs/adr/*.md`, `CHANGELOG.md` 等)に追記する。**無ければ `docs/adr/NNNN-<slug>.md` を新設**(冒頭に `日付: / 状態:` 行を置く)。別名の容器を新設するなら冒頭に `種別: 記録` も置く

既存文書を書き換える前の判別: `docs/adr/`・`CHANGELOG.md` 等の追記型容器の中にある、または冒頭に
`種別: 記録` を持つ文書は追記型 —— 上書きせず「方針が転換したとき」の方式で注記する。
どちらでもなければ直接上書きしてよい。

命名:
- `<YYYY-MM-DD>` は**そのタスクで最初に `work/` に書いた日**。今日の日付ではない。タスクが続く限り変えない
- 着手時に `ls -t work/` を見て、同じ主題の既存ディレクトリがあればそこに追記する
- `<slug>` は英数字とハイフン、2〜5語の名詞句(例: `dashboard-review`, `api-latency-bench`)
- ファイル名は `<内容>.md`。並列・複数観点のときは `<担当>-<内容>.md` で一意にし、互いに上書きしない
- 複数リポジトリに跨るタスクでは、`work/` は作業ディレクトリのリポジトリにだけ作る
- `work/` の外へ出すときの命名は、そのリポジトリの既存規約に従う

昇格(`work/` の成果を残す):
- 既定は **`docs/` 側の該当箇所を書き換える**こと。FBファイル自体を `docs/` に置かない(次項の条件に該当する場合を除く)。
  棄却理由は本文に1文入れる。ID単位の対応表は任意
- 次のいずれかなら、成果物を **`work/` の外へ移し**、`docs/` 側からそれを指して「なぜ残すか」を書く
  (役割の明示でよい: `出典:` `上位:` `レビュー入力:`):
  各指摘への対応を後から追う必要がある / 高リスク設計で検討過程ごと残す /
  **再利用可能な知見・発見を含み、1文に圧縮すると失われる**
  移した先のファイル冒頭に `種別: 記録` と `日付: / 状態:` 行を付す(`docs/adr/` 等の追記型容器の中に出す場合は不要)
  → 完了条件は「移動」「マーカー」「参照文」のすべて
- **`docs/` の文書が `work/` の中身に依存しないこと。** `work/` を丸ごと消しても
  `docs/` の主張が壊れない状態を保つ(所在を1行書くだけなら依存ではない)
- 原文にある記述の昇格は**完全一致の部分文字列**とし、**改行を含まない単位**(原文の1行に収まる範囲)で
  引用して、書いた後に原文へ grep して一致を確認する(要約禁止)。複数行を残したいときは行ごとに分けるか、
  ファイルごと `work/` の外へ出す。昇格時に新しく考えた判断はこの限りでないが、自分の判断と分かるように書く。
  原文に根拠が無ければ「(理由未記録)」と書く
- 完了時は、変更したパスを応答内で1行報告する
- 昇格**しない**もの: 下書き(final が `docs/` にある) / 比較検討の不採択案(理由だけ採択案の本文へ)

方針が転換したとき:
- 追記型の記録は上書きして書き換えない。**当時の本文を残し、該当箇所に「(→ ◯◯ で撤回)」と注記**し、
  冒頭の `状態:` 行を更新する。**結論は撤回するが、その結論に至った根拠は消さない**
  (`状態:` 行が、その記録がいま有効かどうかを示す唯一の場所)

ここに書かれていない種類の成果物が出てきたら、上の既定に戻る —— **迷ったら `work/`**。
新しい置き場や分類ディレクトリを作らない。

例外: 外部ツールが読み取りパスを固定している成果物(cowork の `docs/cowork/<task-id>/intent.md` 等)は
指定パスに置き、記入して commit する。タスク完了時に冒頭へ `種別: 記録` を付す。

作らないもの: `INDEX.md` 等の生成索引 / `work/README.md` / 文書種類ごとの細分類ディレクトリ

`work/` は commit する(失われないため)。削除は任意で、**git 管理下 かつ 自分が完了させたタスク
かつ 昇格済み**のときだけ読まずに消してよい。
```

---

## 12. 導入手順

先行適用の目的は §10-1(スニペットの実効性)と §10-5・§10-6(昇格が実行されるか)の観測であり、
**成立するリポジトリを選ぶ必要がある**。

条件: (a) 権威ある場所への流出が起きた実績があり、(b) 新しい作業が発生すること。

| 候補 | (a) 流出実績 | (b) 稼働 |
|---|---|---|
| **`metsuke`** | **あり**。`docs/` 直下に被参照0件の計画1本 + 登壇原稿1本(計857行) | **あり**(2026-07-25) |
| `muti-repo-workspace` | **あり**。root に3本 + 未追跡2ディレクトリ(21本・3,072行) | **なし**(最終コミット 2026-07-17) |
| `sekisho` | なし(移行候補0本) | あり(2026-07-25) |

**先行適用は `metsuke`。** 両条件を満たす唯一のリポジトリである。
(第2版は metsuke を「流出なし」と書いて `muti-repo-workspace` を選んでいたが、棚卸しが metsuke の
`docs/` 直下に2本の C を挙げており矛盾していた。流出の形は違う —— mrw は root、metsuke は `docs/` 直下 ——
が、どちらも権威ある場所への混入である)

1. `metsuke` に `AGENTS.md`(§11 スニペット)と `CLAUDE.md`(`@AGENTS.md` の1行)を作成する
2. 観測は2系統。**主は git 監査**(パイロット終了時に1回だけ実行する。定期スキャンではない):
   - tracked: `git log --since=2026-07-26 --diff-filter=A --name-only --pretty=format: -- '*.md' | sort -u | grep -v '^work/'`(起点は配布日)
   - untracked: `git status --porcelain -uall | grep '^?? '` で `work/` 外の md を確認(`-uall` が無いと未追跡ディレクトリが `?? dir/` に畳まれ、中の md が見えない。実測故障の `rebuild-doc/` 21本がこの形だった)
   - 補助として、ユーザーレベルの PostToolUse フックが Claude Code の Write|Edit による md 書き込みを全リポジトリで記録する(`~/.claude/artifact-policy-observation/md-writes.jsonl`)。スニペット未配布のリポジトリが対照群になる。**Codex・人手の書き込みはフックを通らない**ため、主計装はあくまで git 監査である

   監査で出た `work/` 外の md は流出とは限らない —— 形態2 の昇格も「`work/` 外への追加」として同じ形で現れる。各ファイルについて §5.1 の参照文の有無を確認し、**昇格 / 流出** に分類してから手順5 の判定に使う。
3. 観測期間: **スニペット配布日から2週間、または新規タスク5件の早い方**。配布は 2026-07-26(→ 〜2026-08-09)。(第4版は制定日起点の「〜2026-08-08」としていたが、配布が済むまで窓が黙って消化されるため配布日起点に改めた)。観測項目: (i) 新しい C 性の成果物が `work/` に着地するか (ii) **昇格が実際に実行されるか**(§10-5) (iii) **知見が形態2 で権威ある場所へ出るか**(§10-6)
4. 昇格の観測には、タスク完了時に `/wrap`(タスク締めのユーザーレベルコマンド。`~/.claude/commands/wrap.md`)を実行する。`work/` の成果物から昇格候補を列挙・提案するリマインドであり、義務化はしない(§9 と整合)
5. 判定は3値。**着地する / 着地しない / 新規作業が発生せず観測できない**。3つ目のときは「成功」と読まないこと
6. `muti-repo-workspace` は稼働が止まっているため観測に使えない。再開したときの2例目とする
7. 着地するなら他リポジトリへ展開。展開時は per-repo の複製ではなく**グローバル1枚**(`~/.claude/CLAUDE.md` + `~/.codex/AGENTS.md`)を第一候補とする(§11 の「複製は腐る」はコピー数を減らすのが根治)。着地しないなら PreToolUse 警告フック(`work/` 外への md 書き込みをエージェントに差し戻す)へ昇格する(PreToolUse フックも PostToolUse と同様 Claude Code にしか効かず、Codex・人手の書き込みには効かない。手順2 の限界がそのまま残る)
