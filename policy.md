# 成果物管理ポリシー

LLMとの作業で生まれる成果物の置き場・権威・寿命の規約。本書だけで運用できる。

日付: 2026-07-25 / 更新: 2026-08-02(第10版) / 状態: **運用可。未検証の前提あり(§10)**

第10版の主な変更(第9巡 = sekisho 移行と全数スイープの適用経験の回収。ユーザー裁定 2026-08-02): **移行の手順詳細・留意事項を [`migration.md`](migration.md) に切り出し**(移行は1リポジトリ1回きりで常時参照されないため。非規範 —— 規範は §12.1 が正で、migration.md は版なしで更新してよい)/ **§12.1 に不変条件2件を追加**(移動前に被参照を全数 grep で列挙 —— 見落としによる壊れリンク11件が実測根拠 / 移行できない対象は名指しで保留し条件ごと記録)/ **§3.3 の射程を「パスを固定」に統一**(読み取り・書き込みを問わない —— skills の `quality/results/` が書き込み側の実例。スニペットの同文言も更新 = スニペット v10)/ **§5.3 に本流の定義を追加**(remote 無し・二層ブランチのリポジトリでは到達をブランチ名で記す)。指摘と裁定は `docs/records/review.md` §15。

第9版の主な変更(第8巡 = 第8版への独立レビュー1本の反映。規範の修正のみ —— スニペット不変): **§12.1 の棚卸し範囲を「root 直下および docs/ 以下の全 md(再帰)」に明確化**(「root・docs 直下」の字義が docs/design/ 等のサブディレクトリの容器外 A を取りこぼす退行だった)/ **§2 の `状態:` 行の読み方を「書かれた時点の有効性判断」に修正**(第8版が書き手の義務を撤廃したのに、読み手への信頼指示が残る非対称があった)/ §9 の「禁止しないもの」を supersede 注記と `状態:` 行に分割(§6 の撤廃根拠との衝突を解消)/ §12.2 の複製探索・鮮度判定を**本流基準**と明記 / §12.1 の手がかり例示語の修正と結合規則の明文化、「`状態:` は付さなくてよい」への軟化 / §4 の `更新:` 行の意味を明記 / §11 に版なし更新可の箇所を名指し。指摘と裁定は `docs/records/review.md` §14。

版履歴は直近2版のみをここに残す。それ以前の変更点は `docs/records/review.md`(各巡の指摘と裁定)と
git tag(`v3` 以降)にある。**第N版は第(N-1)巡レビューを反映したもの**なので、review.md の
該当節は「第(N-1)巡」の見出しで探す(節番号は巡の番号と一致しない。第8版 ← 第7巡 = §13)。
検討の経緯は `docs/records/proposal.md`。

**本書は L(§8)。** 改訂は上書きで行う。`docs/records/`・`docs/adr/` の中になく、冒頭に `種別: 記録` を
持たないため、§2.1 の判別でも L となる。

---

## 1. 目的と適用範囲

**目的**: LLMと作業を進める過程で生まれる成果物(主に md)を、人手の保守を要求せずに管理する。

**適用範囲**: コードリポジトリ、非コードの個人リポジトリ、リポジトリに属さない横断検討のすべて。

**適用時期**: 本ポリシー制定以降の新規作業。既存の散乱は移行しない。
(「移行しない」は**散乱の置き場を動かさない**という意味である。判別機構の変更(第7版)に伴う
既存の容器外 A の1回きりの移動は、散乱の移行ではなく判別の修復であり、§12 の手順で行う)

### 用語

| 語 | 意味 |
|---|---|
| **権威ある場所** | `work/` 以外。そこにある文書は「読んで信じてよいもの」として扱われる |
| **権威** | 現在形の権威(L: いまこうである)と、当時形の権威(A: 当時こう判断した)がある。§2 |
| **L / A / C** | §2 の3分類。「L側」= L に分類される文書、の略記 |
| **昇格** | `work/` の成果を権威ある場所に反映すること。§5 |
| **A の容器** | A を積む先。汎用は **`docs/records/`**、特別容器は **`docs/adr/`**(閉じた集合。§5.6) |
| **`種別: 記録`** | レガシー・ツール固定パスの A シグナル。**新規に書かない・見たら尊重**(§2.1。第6版まではこれが判別子だった) |
| **作業メモ** | 単一タスクに属さない日々のメモ。直近のタスクディレクトリに書く。該当が無ければ `work/<YYYY-MM-DD>-notes/` |

---

## 2. 3分類 — 第一軸は権威

| | 名前 | 権威 | 書かれ方 | 判定質問 |
|---|---|---|---|---|
| **L** | 生きる | **現在形**。「いまこうである」として読んでよい | **上書き**。古くなったら書き換える | 「今後も正しさを維持する責任を負う対象か?」 |
| **A** | 積む | **当時形**。「当時こう判断した」として読んでよい。**既定で当時形として読み、最新性は保証しない**。`状態:` 行があればそれは**書かれた時点の**有効性判断を示す(維持は保証されない —— §6) | 集合としては**追記**。個々のファイルは当時の本文を残し**注記を足す** | 「**時点の記録**か? — 当時の判断と、その根拠となった発見」 |
| **C** | 消える | **無い**。`work/` にあること自体が「信じる対象ではない」の宣言 | 一度書かれ、参照されて役目を終える | 「いまのタスクのために作った道具か?」 |

**A は事実だけでなく解釈を含む。** 「時点の事実」ではなく「時点の記録 = 当時の判断 + その根拠となった発見」である。
再利用可能な知見の大半は事実ではなく解釈であり、A がその受け皿になる。

### 2.1 この分類について正直に書いておくこと

- **C は「終端する」のではなく、最初から権威を持たない。** 第2版は C を「タスク終了で終端」としていたが、
  `work/` は定義上いかなる時点でも権威ある場所ではないので、持ったことのない権威は失えない。
  時点指定は観測可能な差分を持たなかったため撤回した
- **網羅的ではない。** 「上書きされ、かつタスク限りで役目を終える」ものが存在する(複数日タスクの
  `plan.md` など)。これは C に倒す。分類の正しさではなく便宜である
- **「3」であることに論拠は無い。** 実用上この3語で足りているという以上の主張はしない
- **パスが L/A/C のすべてを宣言する(第7版)。** `work/` = C(権威なし)、`docs/records/`・`docs/adr/` = A、
  それ以外 = L。第6版まではパスが宣言するのは権威の有無だけで、L と A はファイル冒頭のマーカーで
  分けていたが、マーカー方式は5巡+実観測(付け忘れ・遡及付与のブランチ滞留)で故障が集中した
  唯一の判別機構だったため、第7版でパスに一本化した(`docs/records/review.md` §12)。
  **移動と分類が同一の操作になる**ため「移したがマーカーを忘れた」という中間状態が存在しない

**既存文書を更新するときの判別**: 次のいずれかに当てはまる文書が **A** —— 上書きせず §6 に従って注記する。

1. `docs/records/**` または `docs/adr/**` の中にある
2. 冒頭メタ行に `種別: 記録` を持つ(**レガシー・ツール固定パスの後方互換**。未移行リポジトリ・
   §3.3 のツール固定パス・第三者の採用リポジトリを保護する。パスと矛盾したら A 側 = 安全側に倒す。
   このマーカーを**新規に書いてはならない** —— 新しい A は容器の中に作れば場所が宣言する)

どちらにも当てはまらなければ **L** —— 直接上書きしてよい。

**`状態:` 行は判別に使わない。** `状態:` は A の記録の現在の有効性を示す行であり(§6)、L の文書が成熟度の表示のために持つこともある(本書自身が例)。第3版は「`状態:` 行の有無」を唯一の判別子としていたが、本書(`状態:` 行を持つ L)を A と誤判定し、`CHANGELOG.md`(`状態:` 行を持たない A の容器)を L と誤判定する。両方向に誤るため第4版で置き換えた。

---

## 3. 置き場

```
<repo>/
├── work/                        C。権威なし(root 直下。docs/ に入れない —— work/ は文書専用ではなく、
│   └── <YYYY-MM-DD>-<slug>/     「work/ 以外 = 権威あり」の一刀を守るため)
└── docs/                        権威ある場所(文書)。無いリポジトリでは作る
    ├── *.md  <topic>/           L — 現在形の本体
    ├── records/                 A の汎用容器(§5.6)
    ├── adr/                     A の特別容器(既存リポジトリのみ。新設しない —— §5.6)
    └── assets/                  L 文書に従属する非文書(画像・データ)
```

(README.md・src/ など docs/ の外の従来ファイルはそのまま L。検討専用リポジトリの形は §8)

**既定は `work/`。** 次の2つに**明確に**当てはまるときだけ、権威ある場所へ書く。当てはまるか迷ったら `work/`。

1. 今後も正しさを維持する責任を負う → L。該当文書を直接書き換える
2. 時点の記録(当時の判断と、その根拠になった発見)で、**今後の判断が依拠しうる**もの
   → A。`docs/records/` に置く(§5.6)。**タスク限りの記録は work/ のまま** —— 時点の記録である
   ことは records/ 行きの十分条件ではない(バグ調査メモ・進捗記録は判定質問を字義で満たすが work/)

**文書以外も同じ。** 使い捨てスクリプト・実行ログ・生成データ・画像も `work/<YYYY-MM-DD>-<slug>/` 配下。
昇格した非文書は、L 文書に従属するなら `docs/assets/`、記録に従属するなら `docs/records/` 内に置く(§4)。

### 3.1 不変条件 — 権威ある場所は `work/` に依存しない

> **権威ある場所の文書は、`work/` の中身に**依存**してはならない。**
> `work/` を丸ごと消しても、権威ある場所の文書の主張が成立しなくなってはいけない。

「依存」とは、その文書を読んで内容を理解したり主張の根拠を確かめたりするために `work/` を
読む必要がある状態を指す。**単なる所在の言及(「〜は `work/` にある」)は依存ではない**
—— リンクが切れるだけで、主張は壊れない。

残す価値があると判断した成果物は §5 形態2 で **`work/` の外へ出す**。これにより
「`work/` は読まずに消せる」が成立する。**ただし無条件ではない**(§7.1)。

### 3.2 新設してよいもの

| | 条件 |
|---|---|
| `work/` | 1リポジトリ1つ(root 直下) |
| `docs/records/`(A の汎用容器) | 1リポジトリ1つ。docs/ ごと無ければ docs/ も作る |
| `docs/assets/` | L 従属の非文書を昇格するとき |

それ以外の置き場は既存の規約を変更しない。**`docs/adr/` の新設は第7版で廃止**した(決定も
`docs/records/` へ。既存の adr/ は存続 —— §5.6)。検討専用リポジトリも同じ構造(§8)。

**`work/README.md`・`records/` 内の README や index は置かない。** 「そのディレクトリがある限り
正しくあり続けねばならない」= L でありながらパスは C / A という自己矛盾になる。名前の意味は
本書と §11 のスニペットが定義し、記録の洗い出しは L 側の手維持対応表で行う(§9)。

### 3.3 例外 — ツールがパスを固定する成果物

外部ツールがパスを固定している成果物は、読み取り・書き込みのどちらを固定しているかを問わず、
そのパスが権威ある場所の中でも、指定パスに置く。
実例: cowork の `docs/cowork/<task-id>/intent.md`(読み取り側 —— ツールはフックのたびにこのパスだけを
読む。移動はコード変更なしにできない)、skills の `quality/results/<日付>/`(書き込み側 ——
評価ハーネスが実行結果を書く記録ディレクトリ。§12.1 の移行対象にもしない)。ただし:

- **未記入テンプレートのまま放置しない。** 置いたら記入し、commit する(untracked 放置は実測故障 #4 の再演)
- **タスク完了時に冒頭へ `種別: 記録` を付す。** 以後は当時の方針の記録(A)として §6 で扱う
- この例外は「ツールがパスを固定している」場合に限る。人が置き場を選べる成果物には適用しない

---

## 4. 命名

- 日付書式は **`YYYY-MM-DD`**(ISO 8601)。`YYYYMMDD` は使わない
- `<YYYY-MM-DD>` は**そのタスクで最初に `work/` に書いた日**。タスクが続く限り変えない。
  **今日の日付ではない。** 3日前に始めたタスクの続きなら3日前の日付のディレクトリに追記する
- 着手時に `ls -t work/` を見て、同じ主題の既存ディレクトリがあればそこに追記する。
  新規作成は該当が無い場合のみ
- `<slug>` は英数字とハイフン、2〜5語の名詞句(`dashboard-review`, `api-latency-bench`)。
  主要名詞は既存ディレクトリ・既存レコードの語彙を再利用する(同じ主題が別名で散ることを防ぐ)
- ファイル名は `<内容>.md`。並列・複数観点のときは `<担当>-<内容>.md` で一意にし、互いに上書きしない
- 複数リポジトリに跨るタスクでは、`work/` は**作業ディレクトリのリポジトリにだけ**作り、
  他方のリポジトリに置き場を示す1行を残すか、作業の応答で明示報告する(次セッションが発見できるように)
- `work/` 内で経緯や気づきを書き足すときは、既存ファイルに追記する。該当が無ければ `notes.md`
- **`docs/records/` のファイルは `<YYYY-MM-DD>-<slug>.md`。** 日付は、`work/` からの昇格なら
  **元の work/ ディレクトリの日付を引き継ぐ**(対応関係が名前で残る)。`work/` を経ない直書きなら
  記録の成立日。並列時は `<YYYY-MM-DD>-<担当>-<slug>.md` で一意にする(A の容器内の上書きは
  検知機構が無いため、命名で衝突を防ぐ)。冒頭に `日付:` 行を置く(更新したら `更新:` を足す —— `更新:` は最新の
  更新日へ書き換える。履歴は git が持つ。`状態:` は任意 —— 義務にしない。§6)。
  記録に従属する資産(画像・データ)は同じ日付-slug 接頭辞で `records/` 内に置く
  (複数なら `records/<YYYY-MM-DD>-<slug>/` のディレクトリ型も可)
- 特別容器(既存の `docs/adr/` 等)へ積むときの命名は、**そのリポジトリの既存規約に従う**
  (`NNNN-<slug>.md` 連番など)。`work/` の命名規則は持ち込まない

---

## 5. 昇格

**昇格とは「C のファイルを L に移すこと」ではない。** 2つの形態がある。

| | 形態 | 行為 | `work/` 側 |
|---|---|---|---|
| **1** | **L の書き換え**(既定) | 本体の該当箇所を書き換え、棄却理由を**本文に1文**入れる | 残る。権威は持たない |
| **2** | **`docs/records/` へ出す** | 成果物を **`docs/records/` へ移し**(§4 の命名)、権威ある場所の文書がそれを指して「なぜ残すか」を書く(§5.3。マーカーは不要 —— 場所が A を宣言する) | **無くなる**(移動する) |

**どちらを選ぶかは、昇格を行う人またはエージェントが決める。** 以下は目安であり、規約が決定するのは
「迷ったら形態1」ということだけである。

形態2 を選ぶ目安:

- レビューの各指摘にどう対応したかを後から追う必要があるとき
- 高リスク設計で検討過程ごと残すと決めたとき
- **再利用可能な知見・発見を含み、1文に圧縮すると失われるとき**(`docs/records/issue.md` L14
  「過程で得られた知見や成果物を適切に管理運用できる状態としたい」の中心。
  第2版はこれを目安に入れておらず、知見が既定で形態1 に流れて失われていた)

複数の目安・§5.5 の行に同時に該当するときは、**知見の有無を優先する**(実測・発見を含む不採択案は
「昇格しない」ではなく形態2)。

**形態2 の宛先は `docs/records/`(内容が決定で、既存の `docs/adr/` があるならそちら)。**
第6版までは「必ずしも A の容器の中ではない」として容器外への昇格を認めていたが、容器外の A は
マーカーが無いと次の更新で上書きされるため(実観測故障)、第7版で宛先を容器内に一本化した。
根拠にしていた実測の2例(`metsuke: docs/08-dashboard-fb.md`、`sekisho: docs/design/synthesis-draft.md`)
は §12 の移行対象である。

### 5.1 昇格が完了したことの検証条件

> **形態2 が完了しているとは、出した先が A の容器(`docs/records/` または特別容器)の中にあり、
> かつ権威ある場所の別の文書から「維持する理由」を付して参照されている状態を指す。**

**これは A 一般の定義ではない。** 適用範囲は次の2つに限る。

| 用途 | 位置づけ |
|---|---|
| (a) **形態2 の完了検証** | 規範。参照文を書き忘れたら形態2 は未完了 |
| (b) **既存ファイルの遡及調査** | **手がかりであって判定ではない。** 権威ある場所にあるものがまだ維持されているかを調べる補助。最終判断は中身を読んで下す |

権威ある場所に**直接書かれた** A(新設した ADR など)には適用しない。被参照0件でも A である。

**「維持する理由」とは何か。** 参照元の内容がその文書に依存している関係が明示されていること
—— すなわち、参照先が変われば参照元も変わるべきだと読める形になっていること。

| 該当する | 該当しない |
|---|---|
| 役割の明示: `出典:` `上位:` `根拠:` `レビュー入力:` | 所在の案内: 「〜にある」「〜に保存」 |
| 保存理由の明記: 「透明性のため保存」 | 参照文が対象を「中間成果物」「作業用」と性格づけている |
| 文書表に目的・内容を付して掲載している | ファイル名を列挙しただけ |

**この判別は機械的ではない。** 参照の有無は `grep` で分かるが、それが依存関係の明示かどうかは
読まないと決まらない。用途(b)で調べるときは、**検索範囲を権威ある場所の全体にすること**
(`docs/` 直下だけでなく `docs/**` すべて)。この範囲を誤って5本を誤判定した実例がある。

### 5.2 粒度

**既定は1文。** 本体の該当箇所を書き換え、棄却理由を1文入れれば足りる。

指摘を ID 単位で対応表にすることは**任意**。これは基準ではなく基準の不在であり、意図的にそうしている
—— 常時の義務にすると、継続できなくなった瞬間に規約全体が崩れるため。

### 5.3 完了条件

| 形態 | 完了条件 |
|---|---|
| 1 | 権威ある場所のファイルが変更されたこと。`work/` 側への変更・削除は含めない |
| 2 | 成果物が `docs/records/`(または特別容器)へ移動し、冒頭に `日付:` 行が付され、**かつ** §5.1 の参照文が書かれたこと。両方が必要(第6版まで要求していたマーカーは場所が A を宣言するため、第7版まで要求していた `状態:` 行は義務でなくなったため、いずれも不要になった) |

**完了の宣言は本流到達後に行う。**「実施済み」と記録・報告してよいのは、変更が本流(master 等)に
到達してからであり、記録は本流側のコミットを指す。根拠は実観測故障: 第5巡の遡及マーキングが
マージ済みブランチに積まれて合流経路を失い、実施記録だけが「実施済み」を主張する状態が生じ、
約2日後に同一パッチの重複適用が起きた。第5巡-1「版番号の発行は master 上の本書に限る」と同じ教訓の、
完了宣言への一般化である。**管理作業(移行・遡及注記・記録だけの追加)は単独の小さな PR で出す**ことを
推奨する(機能開発の完了に人質に取られないため)。タスク内の昇格は機能 PR に同乗してよく、
master 直コミット運用のリポジトリではこの推奨は何も要求しない。

**本流**とは、そのリポジトリの変更が最終的に統合される既定の枝 —— remote があれば origin の
既定ブランチ、無ければ全作業が着地する幹を指す。幹と合流先の二層を持つリポジトリ
(ローカル専用で、作業幹からマイルストーンごとに別ブランチへ合流する運用など)では、
どの枝への到達かを**ブランチ名で記す**(「幹に到達済み・合流先マージ待ち」)。

最後に、変更したファイルのパスを**作業の応答内で1行報告する**(新しいファイルは作らない)。
未マージの時点では「本流到達待ち」と添える。

### 5.4 引用する。要約しない

要約させると、実際には言われていない理由が生成され、権威ある場所に定着する。

- **原文にある記述を昇格するとき**(指摘の内容、実測値、発言)は、原文からの**完全一致の部分文字列**とし、**改行を含まない単位**(原文の1行に収まる範囲)で引用する。書き込み後、原文へ grep して一致を確認する。複数行にわたる内容を残したいときは、引用を行ごとに分けるか、形態2(ファイルごと出す)を使う(md の折り返しを跨ぐ文字列は行単位の grep で照合できないため)
- **昇格時に新しく生成される判断**(なぜその指摘を採らないか等)は原文に存在しないので、この規律の対象外。
  ただし**その判断が自分のものであることが分かるように書く**(「(本対応時の判断)」等)
- 原文にあるはずの根拠が見つからなければ「(理由未記録)」と書く

**この手続きは出所を確認するだけで、引用が適切であることは保証しない。** 無関係な一致でも通る。
検出できるのは「要約が混入した」場合だけである。

### 5.5 昇格の例(規約ではなく例示)

**これは分類のタクソノミーではない。** 置き場の分類は §2 の3分類だけであり、以下は §5 の適用例。
ここに無い種類が出てきたら §3 の既定(= `work/`)と §5 の目安に立ち返る。

| 例 | 昇格 |
|---|---|
| レビューFB | 形態1が既定 |
| 比較検討の候補案 | 採択案を L に書き、**不採択の理由を採択案の本文に1文**。不採択案のファイルは昇格しない(**ただし実測・発見を含むなら形態2** —— 知見の有無が優先。§5) |
| 計画 | 実装が終われば消費済み。L に残すのは**結果として何がそうなったか**だけ |
| 調査結果・発見 | **形態2**(§5 の目安3)。1文に圧縮すると知見が失われるため |
| 下書き | 昇格しない。final が L にある |
| 事実と解釈が混在する findings | **分離しない。ファイル単位で扱う** |

レコードの書式の目安(冒頭の `日付:` 行だけが必須。本文の型は自由): レビューFB は
「指摘 / 判断・反映先」、決定は「決定 / 背景 / 根拠となった発見 / 影響」、調査 findings は
「問い / 発見 / 根拠」。型を迷う時間を減らすための決め打ちであり、置き場の分類ではない(§9 と矛盾しない)。

### 5.6 A の容器 — records/ と特別容器

**A の汎用容器は `docs/records/` である。** 無ければ新設する(docs/ ごと。§3.2)。
1リポジトリにつき一度作れば、以後は §4 の命名で置くだけになる。

「人が作るファイルを増やさない」は**毎回増やさない**という意味であって、**一度も作らない**ではない。
容器が無いまま知見を `work/` に書くと、それは権威を持たない側に残る。それでは知見を残す目的を満たせない。

**特別容器は「多くのシチュエーションで必要」が実証された種別に限る閉じた集合**とし、本書が版として
定義する。リポジトリ・タスク単位で新設しない。現時点の集合は **`docs/adr/` のみ**
(全リポジトリ横断で発生する「決定」という種別・業界標準としての知名度・実測中最良の実践)。
将来の追加基準: (i) 複数リポジトリで反復して発生 (ii) 種別固有の書式・参照規約が確立
(iii) 本書の改訂(版)としてのみ追加する。

- `docs/adr/` が**既に在る**リポジトリでは、決定は従来どおりそこへ積む(連番 `NNNN` は既存の最大値+1。
  **並列時の採番・`records/` の新設は親エージェントのみが行う**)。無いリポジトリでは決定も
  `docs/records/` の日付レコードとして書く —— **`docs/adr/` を新設しない**(第3〜6版の新設規定を廃止)
- ad-hoc の容器(`docs/BENCH.md` 等の名詞句追記ファイル)は作らない。実測値の蓄積のような集合型の
  内容は「1計測 = 1日付レコード」+ **L 側の手維持集計表**に分解する(1ファイル肥大を避ける。
  第6版まで認めていた既知名以外の容器新設は、判別の複雑化とファイル肥大のため第7版で廃止した)

---

## 6. 方針転換の記録

> **A の記録は上書きして書き換えない。方針が転換したら、当時の本文を残したまま、
> 該当箇所に「(→ ◯◯ で撤回)」と注記する。**

`metsuke/docs/adr/0011-local-dashboard.md:8` に「本文は当時の決定として残し、該当箇所に注記する」と
既に明文化され、`0003(作らない) → 0011(部分置換) → 0012(S3のみ撤回)` の連鎖が本文を読むだけで追える。

```
日付: 2026-07-17 / 状態: **部分的に置換**（2026-07-21・[ADR 0011](0011-local-dashboard.md)）
日付: 2026-07-21 / 更新: 2026-07-25 / 状態: 採択・実装済み（S3は[ADR 0012](…)が撤回）

本文中の該当箇所:  **（→ [ADR 0012](…) で撤回）**
```

**結論は撤回するが、その結論に至った根拠は消さない。** 発見(= 知見)は当時の記録として残す。

**記録は既定で当時形として読む。最新性・有効性の追跡は約束しない。** A の権威は当時形なので(§2)、
本文をそのまま「いま有効」と読んではならない。いつの記録かは `日付:` 行・ファイル名の日付・git が示す。
`状態:` 行は第7版まで必須(有効性を示す唯一の場所)だったが、**第8版で義務から外した** ——
状態の常時更新は保守義務であり、剪定されなければ現在形で嘘をつく(§9 が索引を禁じるのと同じ構造。
転換は新しい記録が起きた側に書かれるので、古い記録側の更新は忘れられやすい)。
`状態:` 行を持つ記録(上の ADR 等の既存慣行)ではそれを更新してよく、その慣行は妨げない。

**まだ A が存在しない段階(`work/` で作業中)で方針が変わったとき**は、`work/` の中で経緯を書き足せばよい。
注記が必要になるのは権威ある場所へ出して以降である。

---

## 7. 削除

**権威の有無(§2)と削除(ファイルを消すこと)は別である。** C は `work/` にあることで既に
「信じる対象ではない」と宣言されており、これは**何もしなくても成立している**。

そのうえで、**削除は任意。義務にしない。** 義務が果たされなかったとき、残存物が
「価値があるから残っている」と誤読されるため。

| 方法 | 条件 |
|---|---|
| **何もしない** | **既定**。`work/<YYYY-MM-DD>-<slug>/` は日付付きなので `ls work/` がそのまま索引になる |
| 削除する | §7.1 の条件を満たす場合 |
| `archived/` へ移す | git 管理外でも可。完了時に**まとめて**動かす(定期スキャンはしない) |

### 7.1 「読まずに消せる」が成立する条件

§3.1 の不変条件は「読まずに消せる」を目指すが、**無条件ではない**。次の3つを満たすときに限る。

1. **git 管理下であること。** 管理外なら唯一のコピーが消える(§7.2)
2. **そのタスクが完了していること。** 継続中のタスクのディレクトリかどうかは**パスから分からない**
   (§4 は3日前のディレクトリへの追記を認めている)
3. **昇格が実施済みであること。** 未昇格なら `work/` が唯一のコピーである。
   §10-5 のとおり、昇格の未実施は本ポリシー自身が最も起こると予測している故障モードである
4. **そのディレクトリに自分以外の書き込みが無いこと**(`git log -- <dir>` で確認。
   並列エージェント・他セッションの成果を巻き添えにしないため)

4条件のうち2〜4はパスに書かれていないので、**「読まずに」消せるのは自分が完了させたタスクだけ**である。

### 7.2 git 前提

**git 管理下でない場所では `work/` の中身を消さない。** 消したいなら先に `git init` するか `archived/` へ移す。

**`work/` は commit する。理由は「失われないため」である。**
gitignore すると、そのタスクで得た記録がバックアップも共有もされないまま消えうる。
(第2版はこれを「gitignore すると untracked litter になる」と説明していたが誤り。gitignore すれば
定義上 litter ではなくなる。実測の放置事例は gitignore されていなかったので、gitignore の害の証拠でもない)

**`work/` は蓄積する。** 本ポリシーが解くのは「何が権威あるものか」の混乱であって、蓄積そのものではない。
蓄積が無害だとは主張しない(§10-4)。

---

## 8. リポジトリに属さない検討

> **リポジトリに属さない検討は、リポジトリの親ディレクトリに置かず、検討専用リポジトリを作る。**

構造は通常のリポジトリと**同一**である(第7版で統一。第6版までの「直下に A を置き `docs/adr/` を
作らない」特例は廃止し、本リポジトリ自身の A 3本も `docs/records/` へ移した):

```
<検討専用repo>/
├── <主題>.md  README.md                  L(現在形の権威。リポジトリ直下)
├── docs/records/                         A(当時形の権威)
└── work/<YYYY-MM-DD>-<slug>/             C
```

移行で入った既存レコードは名詞句のファイル名のまま。新しい記録は §4 の `<YYYY-MM-DD>-<slug>.md` で作る。

(`issue.md` は第6版で L から A に確定した。課題認識は規約の導出元であり、権威ある場所の8箇所以上が
行番号で参照する凍結された時点の記録だからである —— L のままだと上書き改訂の瞬間にアンカーが
静かに壊れる。第7版で `docs/records/` へ移動したが、内容は不変なので行番号アンカーは保存される。
裁定は `docs/records/review.md` §11・§12)

移動する前に、そのファイルが他所から参照されていないか確認する
(孤児に見えて生きた依存であることがある)。

---

## 9. 作らないもの

| 作らないもの | 理由 | 根拠の強さ |
|---|---|---|
| 文書種類の細分類タクソノミー | 増える。エージェントが迷い、迷ったとき権威ある場所に書く | **強い**。実測17種。反証されなかった |
| 削除の義務化 | 義務が果たされないとき、残存物が「価値がある」と誤読される | 中。§7 の設計と一貫 |
| **生成される**索引(`INDEX.md` / `timeline.md`) | 剪定されないまま現在形で嘘をつき、嘘だと検知できない | **弱い**。剪定されて未対応項目ゼロに到達した反例がある。絶対視しない |
| `work/` の gitignore | 記録が失われる(§7.2) | 中 |
| 定期棚卸し lint / cron | 保守を要求する | **弱い**。実測サンプルに lint/cron の実例は0件で「実証済み」とは言えない |

**手で維持する README の文書表は対象外**(むしろ §5.1 の参照文の受け皿になる)。
ただしこれも腐る。腐れば §5.1 用途(b)の手がかりも狂う —— だから用途(b)は判定ではなく手がかりに留めてある。

**§10-2 の誤 A を許容しつつ索引を禁じるのは非対称に見えるが、害の及ぶ範囲が違う。**
誤 A は1つの成果物についての主張であり、その文書を読めば嘘だと分かる。索引は集合全体についての
主張であり、**何が抜けているかは読んでも分からない**。検知可能性が違う。

**禁止しないもの**: 文書内の supersede 注記と `状態:` 行(§6)。supersede 注記は書かれた事実の記述なので
剪定漏れで嘘をつかない。`状態:` 行は現在形の主張なので陳腐化で嘘をつき得るが、害は当該1文書に
局所化し、本文を読めば検知できる —— だから禁止まではしない(義務にもしない —— §6)。
いずれも索引と違い、集合全体については主張しない。

---

## 10. 既知の限界と未検証の前提

**「限界として開示したから免責される」わけではない**ので、それぞれ何に該当するかを明示する。

| # | 事項 | 分類 | 内容 |
|---|---|---|---|
| 1 | スニペット1枚で置き場が変わる | **前提(部分的に検証済み)** | 第6巡で「着地する」を観測(新規タスク2件が work/ に着地・C の流出0件。対照群 sekisho は work/ 使用0件のまま)。ただし単一リポジトリ・約1週間・n=2 の観測であり、一般化はしない。`docs/records/review.md` §12 |
| 2 | §5.1 用途(b)の精度 | **規則の弱さ** | 参照は「維持する意思」の代理指標であり、参照元が単に更新されていないだけの場合を区別できない(`mrw/plan.md` が実例)。害は「消さないこと」ではなく、権威ある場所に偽の「なぜ残すか」が定着すること。ゆえに用途(b)は判定ではなく手がかりに留める(§5.1) |
| 3 | 3分類の残余 | **真の限界** | 「上書きされ、かつタスク限りで役目を終える」ものは C に倒す。C は消さなくてよいので実害は生じない |
| 4 | 蓄積は解かない | **適用範囲の縮小** | `docs/records/issue.md` の「管理しづらい」は (i) 権威ある場所への混入 と (ii) 蓄積 の2成分あり、本ポリシーは (i) だけを解く |
| 5 | 昇格の手間 | **設計自身が予測する故障モード** | 昇格は書き込み時より判断も手間も多い。判断を消したのではなく書き込み時から昇格時へ**移した**。最初に破られるのはここである |
| 6 | **知見の保存は昇格に依存する** | **#5 の帰結** | `docs/records/issue.md` L14 について本ポリシーが用意したのは**経路**であって保証ではない。昇格が実行されなければ知見は `work/` に残り、権威を持たないままになる。「L14 を解決した」とは言わない。第6巡の観測では形態1 が1件実行され、形態2 は観測できなかった(機会の不発生) |
| 7 | マーカーの読み手互換 | **規則の残余** | 「パスに一本化」は**書く側**の主張である。読む側は §2.1 のレガシーマーカーを尊重し続ける(未移行・第三者リポジトリ・§3.3 の保護。誤る方向は「L を A と扱う」= 安全側) |
| 8 | CHANGELOG の当時形保護の喪失 | **受容したトレードオフ** | 第7版でファイル型容器を廃止したため、CHANGELOG のリリース済み節は規約上 L(上書き可)になる。実測サンプルに実運用の CHANGELOG が無いことが受容の根拠。守りたいリポジトリはリリースごとに records/ へ切り出す実践を任意で採れる |
| 9 | スニペットの二重読み込み | **受容したコスト** | 公開リポジトリではグローバル1枚と per-repo 併置(§12.3)により、セッションのコンテキストにスニペットが2回載る。内容は同一で矛盾しないが、版更新の過渡期に食い違う窓がある(§12.2 の同時更新で管理) |
| 10 | 記録の最新性は保証されない | **設計上の選択(第8版)** | `状態:` 行の義務を撤廃したため、覆された記録がそれと分かる表示を持つとは限らない。読者は記録を常に当時形として読む(§2・§6)。転換の事実は新しい記録・L 側の対応表・本文への撤回注記が担う |

---

## 11. 配布用 AGENTS.md スニペット

各リポジトリの **`AGENTS.md`** に貼り、`CLAUDE.md` には `@AGENTS.md` の1行を置く。これが実効性の本体。
`AGENTS.md` を正とするのは、実装を委譲している Codex が `CLAUDE.md` を読まないため(Codex が読むのは
`AGENTS.md`。Claude Code は `CLAUDE.md` の `@AGENTS.md` import で同じ内容を読む)。実体は1リポジトリ1枚に保たれる。

**スニペットは本書からの複製である。** §11 のブロックが改訂されると配布済みのコピーは古くなる ——
これは §9 が索引を禁じる理由と同じ構造を持つ。したがって**先頭に版を書き、古いことを検知可能にする**。
検知できない嘘は作らない、が §9 の趣旨であり、版付きの複製はその条件を満たす。
(本書の他の節だけを改訂した版ではコピーは古くならない。次の運用規則の4番目のとおり)

**版の運用規則**(第5版で配布コピーが本体を追い越す逆転が実際に起き、上の検知設計が
「コピーが古い」方向しか想定していなかったため新設。経緯は `docs/records/review.md` §11):

- 版番号の発行は **master 上の本書**に限る。ブランチや worktree 上の改訂は、master に
  マージされるまで版を名乗らない
- 配布するのは master にマージ済みの版だけ。**改訂 → タグ付与 → 配布は同一の流れで行う**
- 配布コピーの版が本体の版より新しい状態は規約違反である。発見したらコピーを「修復」せず、
  **本体への統合を先に行う**
- スニペットの版ヘッダは、スニペットの内容が最後に変わった版を指す。本体の版とは独立に動く
  (本体だけの改訂ではヘッダは上がらない)
- 版の権威は本書冒頭の版表記である。git tag(`v3` 以降)は照合の便宜であり、タグの欠落は
  版を無効にしない
- **版を要するのは規範(従うべき規則)の追加・変更である。** 運用事実の更新は版を要さない ——
  版なしで更新してよい箇所は名指しで限る: §12 冒頭のパイロット結果 / §12.2 の複製一覧 /
  §12.3 の実施状態 / §10 の実測値 / `migration.md`(非規範の留意事項 —— 規範は §12.1 が正)。これ以外の本文に触れる変更と、迷う変更は版を上げる側に倒す
  (第7版の後、§12.3 の併置基準が版なしで追加された境界事例があり、第8版で追認した ——
  この基準自体がその再発防止である)

```markdown
<!-- artifact-policy v10 (2026-08-02) — 本体: github.com/taku7777777/agentic-coding-policy/policy.md -->
## 作業成果物の置き場

**既定は `work/<YYYY-MM-DD>-<slug>/`。** 迷ったらここ。
対象: 調査結果 / レビューFB / 比較検討の候補案 / 下書き / 計画 / 作業メモ / 引き継ぎメモ、
および文書以外の中間物(使い捨てスクリプト・実行ログ・生成データ・画像)。

分類はパスで決まる:
- `docs/records/**`・`docs/adr/**` の中、**または冒頭に `種別: 記録` を持つ**文書 = **時点の記録**。
  上書きせず「方針が転換したとき」の方式で注記する(マーカーは**新規に書かない・見たら尊重**。
  パスと矛盾したら記録側に倒す)
- `work/**` = 作業中間物。権威なし
- それ以外 = 現在形の文書。直接上書きしてよい

`work/` の外(権威ある場所)へ書くのは、次の2つに**明確に**当てはまるときだけ:
1. 今後も正しさを維持する責任を負う → `docs/` 等の該当文書を直接書き換える
2. 時点の記録(当時の判断と、その根拠になった発見)で、**今後の判断が依拠しうる**もの
   → `docs/records/<YYYY-MM-DD>-<slug>.md`(無ければ docs/records/ を作る。冒頭に `日付:` 行。
   更新したら `更新:` を足す)。
   決定の連番容器(`docs/adr/`)が既に在るリポジトリでは、決定はそちらに積む。
   **タスク限りの記録は `work/` のまま**(時点の記録であることは records/ 行きの十分条件ではない)

命名:
- `work/` の `<YYYY-MM-DD>` は**そのタスクで最初に `work/` に書いた日**。今日の日付ではない。タスクが続く限り変えない
- 着手時に `ls -t work/` を見て、同じ主題の既存ディレクトリがあればそこに追記する
- `<slug>` は英数字とハイフン、2〜5語の名詞句(例: `dashboard-review`)。主要名詞は既存の語彙を再利用する
- ファイル名は `<内容>.md`。並列・複数観点のときは `<担当>-<内容>.md` で一意にし、互いに上書きしない
- `records/` のファイル名は `<YYYY-MM-DD>-<slug>.md`(`work/` からの昇格は元ディレクトリの日付を引き継ぐ。
  並列時は `<YYYY-MM-DD>-<担当>-<slug>.md`)。記録に従属する画像・データも同じ接頭辞で `records/` 内へ。
  現在形の文書に従属する資産は `docs/assets/`
- `docs/adr/` の連番は既存最大値+1。**並列時の採番・`records/` の新設は親エージェントのみ**が行う
- 複数リポジトリに跨るタスクでは、`work/` は作業ディレクトリのリポジトリにだけ作り、
  他方のリポジトリに置き場を示す1行を残すか、応答で明示報告する

昇格(`work/` の成果を残す):
- 既定は **`docs/` 側の該当箇所を書き換える**こと。棄却理由は本文に1文入れる。ID単位の対応表は任意
- 次のいずれかなら、成果物を **`docs/records/` へ移し**、`docs/` 側からそれを指して「なぜ残すか」を書く
  (役割の明示でよい: `出典:` `上位:` `レビュー入力:`):
  各指摘への対応を後から追う必要がある / 高リスク設計で検討過程ごと残す /
  **再利用可能な知見・発見を含み、1文に圧縮すると失われる**
  → 完了条件は「移動」と「参照文」の両方。**「実施済み」と記録・報告してよいのは本流(master 等)到達後**
  (未マージなら「本流到達待ち」と添える)。記録・注記だけの管理変更は単独の小さな PR で出す
- 複数に該当するときは**知見の有無を優先**する(実測・発見を含む不採択案は「昇格しない」ではなく records/ へ)
- **`docs/` の文書が `work/` の中身に依存しないこと。** `work/` を丸ごと消しても
  `docs/` の主張が壊れない状態を保つ(所在を1行書くだけなら依存ではない)
- 原文にある記述の昇格は**完全一致の部分文字列**とし、**改行を含まない単位**(原文の1行に収まる範囲)で
  引用して、書いた後に原文へ grep して一致を確認する(要約禁止)。複数行を残したいときは行ごとに分けるか、
  ファイルごと `records/` へ出す。昇格時に新しく考えた判断はこの限りでないが、自分の判断と分かるように書く。
  原文に根拠が無ければ「(理由未記録)」と書く
- 完了時は、変更したパスを応答内で1行報告する
- 昇格**しない**もの: 下書き(final が `docs/` にある) / 知見を含まない不採択案(理由だけ採択案の本文へ)

方針が転換したとき:
- 時点の記録は上書きして書き換えない。**当時の本文を残し、該当箇所に「(→ ◯◯ で撤回)」と注記**する。
  **結論は撤回するが、その結論に至った根拠は消さない**。記録は既定で当時形として読む
  (最新性は保証しない。`状態:` 行を持つ記録ではそれも更新してよい)

ここに書かれていない種類の成果物が出てきたら、上の既定に戻る —— **迷ったら `work/`**。
`records/` 以外の新しい置き場・容器・分類ディレクトリを作らない。

例外: 外部ツールがパスを固定している成果物(読み書きを問わない。cowork の `docs/cowork/<task-id>/intent.md` 等)は
指定パスに置き、記入して commit する。タスク完了時に冒頭へ `種別: 記録` を付す(この場合だけ書く)。

作らないもの: `INDEX.md` 等の生成索引 / `work/README.md`・`records/` 内の README / 文書種類ごとの細分類ディレクトリ

`work/` は commit する(失われないため)。削除は任意で、**git 管理下 かつ 自分が完了させたタスク
かつ 昇格済み かつ 自分以外の書き込みが無い**ときだけ読まずに消してよい。
```

---

## 12. 導入と移行

**パイロット(§10-1 の観測)は 2026-08-01 に完了した。** 判定は「**着地する**」
((i) 新規タスク2件が work/ に着地・C の権威側流出0件 (ii) 形態1 の昇格1件
(iii) 形態2 は観測できない = 機会の不発生)。観測期間は「配布日から2週間または新規タスク5件の早い方」
(第6版 §12-3)で、**新規タスク5件条件の充足**(配布後のマージ済み PR #12〜#16)により期日 08-08 を
待たず終了時監査を実施した。監査手順・除外事項・裁定の全体は `docs/records/review.md` §12。

### 12.1 導入手順(1リポジトリ)

1. §11 のスニペットを対象リポジトリの **`AGENTS.md`** にコピーし、`CLAUDE.md` に `@AGENTS.md` の
   1行を置く。コピーの鮮度は、コピーの本文と §11 ブロックの `diff` で確かめる
2. **既存の容器外 A の1回きり移行**を配布と同じ流れで行う:
   - 全数列挙は**内容判定が主**。root 直下および docs/ 以下の**全 md(再帰。ただし `records/`・`adr/` の
     中と §3.3 の固定パスは除く)**を3つの手がかりで棚卸しする:
     (i) **役割宣言** —— README の文書表や文書自身の位置づけ行(「実測記録」「レビュー」「当時の決定」等。
     「最新版」のような現在形の宣言はむしろ L の信号)
     (ii) **履歴の不変性** —— 初版コミット以来変更が無い = 凍結の実態
     (iii) §2 の判定質問「時点の記録か」と §3 の射程フィルタ。
     (i)(ii) はいずれかが立てば候補に挙げ、**(iii) で確定する**(手がかり単独で A と確定しない)。
     `grep -rl "種別: 記録" --include='*.md' .` は**補助**(マーカーは旧規約が配布済みだった
     リポジトリにしか存在しない —— 新規導入3リポジトリでの実測は0件だった)。
     固定リストを使わない(列挙から漏れた容器外 A は静かに L 化するため、必ずその場で全数を出す)
   - 移行ファイルが冒頭に `日付:` 行を欠くときは付す(値は初版コミット日)。`状態:` は付さなくてよい(義務でないため。付すことも妨げない —— §6)
   - 移動の前に、移動対象のファイル名(basename)を repo 全体で grep して**被参照を全数列挙**する
     (主要な L 文書だけを見ると活動中の文書からの流入参照を見落とす —— 実測で壊れリンク11件。
     上の全数列挙と同じ原理: 発見は列挙でなく走査で行う)
   - 各ファイルを `git mv` で `docs/records/` へ(ファイル名は保持。日付接頭辞は新規レコードから)。
     L 側の参照リンクを更新し、A 本文内で壊れる相対リンクだけを機械的に修正して日付付きで注記する(§6 の例外)
   - 管理作業なので**単独の小さな PR** で出し、**本流到達を確認して完了**とする(§5.3)。
     未マージのブランチが対象ファイルに触れている場合は、先にそのブランチを合流させてから移行する
     (rename/modify 衝突で旧パスにファイルが復活するのを防ぐ)。移行できない対象
     (活動中・未マージが触れる等)は**名指しで保留**し、保留条件ごと実施記録に残す
     (全数列挙 → 「移行」または「名指し保留」の二値で閉じる)
   - 移行済みファイル内の既存 `種別: 記録` 行は残置してよい(§2.1 の読み手互換により無害)
   - ツールがパスを固定する成果物(§3.3)は移行しない
   - 手順の詳細・並行作業時の留意事項・リンク修正のパターンは **[`migration.md`](migration.md)** を
     併読する(非規範の運用ガイド —— 規範は本節が正。§11 により版なしで更新してよい)

### 12.2 複製の一覧

版を更新したときに更新する複製: **§11 スニペットブロック / グローバル2枚(`~/.claude/CLAUDE.md` 内の
ブロック・`~/.codex/AGENTS.md`)/ 公開リポジトリの `AGENTS.md`(現在: `metsuke`・`preference-analyzer`・`co-working-workflow`・`skills`・`claude-code-sandbox-experiments`・`muti-repo-workspace`)/
`~/.claude/commands/wrap.md`(スニペット仕様への参照)**。自リポジトリの `AGENTS.md` は参照設計で
版ヘッダを持たない(§8 の差分だけを書く)。この一覧は手がかりであって網羅の保証ではない ——
複製の発見は `grep -rl "artifact-policy v" ~/.claude/commands ~/.claude/CLAUDE.md ~/.codex/AGENTS.md ~/github/*/AGENTS.md` で行い(探索先を限定するのはセッションログ等の偽陽性を避けるため)、鮮度は内容の diff で判定する(版番号の不一致は
古さを意味しない。違反はコピー版 > 本体版の方向だけ —— §11 の版運用規則)。
探索と鮮度判定は各リポジトリの**本流**(origin/master 等)の内容で行う —— 作業ツリーは checkout 中のブランチ次第で「偽の古さ」や「偽の不在」を返す(§5.3 と同じ理由)。

### 12.3 展開(2026-08-01 実施 —— グローバル1枚)

**グローバル1枚を実施した**(ユーザー裁定 2026-08-01)。スニペットを `~/.claude/CLAUDE.md`
(Claude Code が全リポジトリで読む)と `~/.codex/AGENTS.md`(Codex 同)に配置。以後、
自分の新しいリポジトリには**配布作業なしで規約が効く**(per-repo 配布の忘れが構造的に消える)。

**公開リポジトリには per-repo の `AGENTS.md` を併置する。** グローバル1枚はこの端末の外に
旅をしないため、第三者がリポジトリを clone したとき規約が見えるのは repo 内のコピーだけである。
基準: 他者がそのリポジトリ単体を利用する可能性があるか。非公開・個人リポジトリへの per-repo 配布は不要。

補助計装(PostToolUse フック `artifact-policy-log.sh`)は観測の目的を終えており、停止はユーザー判断でよい。

(第4〜6版の観測計装の詳細 —— 監査コマンド・観測期間の確定経緯・遡及マーキング規定 —— は
役目を終えたため本欄から除いた。全文は git tag `v6` の本書 §12 と、`docs/records/review.md` §9〜§12 にある)
